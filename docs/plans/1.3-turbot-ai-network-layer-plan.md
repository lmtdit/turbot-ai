# Turbot-AI 网络层实现计划

## 1. 概述

本计划是 Turbot-AI 项目的第三阶段实现，旨在建立完整的网络通信层。网络层为整个项目提供HTTP客户端和WebSocket支持，用于与AI提供商API进行通信。

### 1.1 目标

- 实现HTTP客户端（基于libcurl）
- 实现WebSocket客户端
- 支持异步网络IO（基于Asio）
- 支持流式响应处理
- 支持连接池和重试机制

### 1.2 依赖关系

- **前置依赖**: 1.1 - 基础架构计划
- **后置依赖**: Provider系统、Server应用

### 1.3 交付物

- HTTP客户端实现
- WebSocket客户端实现
- 异步网络IO层
- 连接池
- 单元测试和集成测试

## 2. 架构设计

### 2.1 分层架构

```
┌─────────────────────────────────────┐
│      Provider层 (OpenAI等)           │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      网络抽象层 (HTTP/WebSocket)     │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      网络实现层 (libcurl/Asio)       │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      操作系统网络层 (TCP/SSL)        │
└─────────────────────────────────────┘
```

### 2.2 目录结构

```
libs/
└── network/
    ├── CMakeLists.txt
    ├── http/
    │   ├── CMakeLists.txt
    │   ├── http_client.hpp
    │   ├── http_client.cpp
    │   ├── http_request.hpp
    │   ├── http_response.hpp
    │   └── http_headers.hpp
    ├── websocket/
    │   ├── CMakeLists.txt
    │   ├── websocket_client.hpp
    │   └── websocket_client.cpp
    └── async/
        ├── CMakeLists.txt
        ├── async_io.hpp
        └── async_io.cpp
```

## 3. 技术栈

| 类别 | 库 | 版本 | 用途 |
|------|-----|------|------|
| HTTP客户端 | libcurl | 8.x | HTTP/HTTPS请求 |
| 异步IO | Asio | 1.28+ | 异步网络操作 |
| 加密 | OpenSSL | 3.x | SSL/TLS支持 |

## 4. 核心接口设计

### 4.1 HTTP客户端

**文件**: `libs/network/http/http_client.hpp`

```cpp
namespace turbot::network::http {

struct HttpRequest {
    std::string method;  // GET, POST, PUT, DELETE
    std::string url;
    std::vector<std::pair<std::string, std::string>> headers;
    std::string body;
    int timeout = 30;  // 秒
    bool follow_redirects = true;
    int max_redirects = 5;
};

struct HttpResponse {
    int status_code;
    std::string status_message;
    std::vector<std::pair<std::string, std::string>> headers;
    std::string body;

    // 获取响应头
    std::optional<std::string> get_header(const std::string& name) const;

    // 判断是否成功
    bool is_success() const { return status_code >= 200 && status_code < 300; }
};

// 流式响应
struct StreamResponse {
    int status_code;
    std::vector<std::pair<std::string, std::string>> headers;
    AsyncGenerator<std::string> body_stream;

    bool is_success() const { return status_code >= 200 && status_code < 300; }
};

class HttpClient {
public:
    HttpClient();
    ~HttpClient();

    // 同步请求
    Task<HttpResponse> request(const HttpRequest& req);

    // 异步请求
    Task<HttpResponse> get(const std::string& url,
                            const std::vector<std::pair<std::string, std::string>>& headers = {});

    Task<HttpResponse> post(const std::string& url,
                             const nlohmann::json& body,
                             const std::vector<std::pair<std::string, std::string>>& headers = {});

    // 流式请求
    Task<StreamResponse> request_stream(const HttpRequest& req);

    // 设置默认超时
    void set_default_timeout(int timeout) { default_timeout_ = timeout; }

    // 设置默认头
    void set_default_header(const std::string& name, const std::string& value);

    // 设置代理
    void set_proxy(const std::string& proxy);

private:
    int default_timeout_ = 30;
    std::unordered_map<std::string, std::string> default_headers_;
    std::string proxy_;
};

} // namespace turbot::network::http
```

### 4.2 WebSocket客户端

**文件**: `libs/network/websocket/websocket_client.hpp`

```cpp
namespace turbot::network::websocket {

enum class WebSocketState {
    Connecting,
    Open,
    Closing,
    Closed
};

struct WebSocketMessage {
    enum class Type { Text, Binary, Close, Ping, Pong } type;
    std::string data;
};

class WebSocketClient {
public:
    using MessageHandler = std::function<void(const WebSocketMessage&)>;
    using CloseHandler = std::function<void(int code, const std::string& reason)>;

    WebSocketClient();
    ~WebSocketClient();

    // 连接WebSocket
    Task<bool> connect(const std::string& url,
                       const std::vector<std::pair<std::string, std::string>>& headers = {});

    // 断开连接
    Task<void> close(int code = 1000, const std::string& reason = "");

    // 发送消息
    Task<void> send(const std::string& message);
    Task<void> send_binary(const std::vector<uint8_t>& data);

    // 设置消息处理器
    void on_message(MessageHandler handler) { message_handler_ = handler; }

    // 设置关闭处理器
    void on_close(CloseHandler handler) { close_handler_ = handler; }

    // 获取状态
    WebSocketState state() const { return state_; }

    // 是否已连接
    bool is_connected() const { return state_ == WebSocketState::Open; }

private:
    WebSocketState state_ = WebSocketState::Closed;
    MessageHandler message_handler_;
    CloseHandler close_handler_;
    std::unique_ptr<asio::io_context> io_context_;
    std::unique_ptr<websocket::stream<tcp::socket>> ws_;
};

} // namespace turbot::network::websocket
```

### 4.3 异步IO

**文件**: `libs/network/async/async_io.hpp`

```cpp
namespace turbot::network::async {

class AsyncIO {
public:
    // 单例访问
    static AsyncIO& instance();

    // 运行IO上下文
    void run();

    // 停止IO上下文
    void stop();

    // 获取IO上下文
    asio::io_context& context() { return *io_context_; }

    // 提交任务
    template<typename F>
    auto post(F&& f) -> asio::awaitable<std::invoke_result_t<F>>;

    // 延迟执行
    Task<void> delay(std::chrono::milliseconds duration);

private:
    AsyncIO();
    std::unique_ptr<asio::io_context> io_context_;
    std::unique_ptr<asio::executor_work_guard<asio::io_context::executor_type>> work_;
};

} // namespace turbot::network::async
```

## 5. 实现要点

### 5.1 HTTP客户端实现

```cpp
Task<HttpResponse> HttpClient::request(const HttpRequest& req) {
    HttpResponse response;

    // 创建libcurl句柄
    CURL* curl = curl_easy_init();
    if (!curl) {
        throw std::runtime_error("Failed to initialize curl");
    }

    // 设置URL
    curl_easy_setopt(curl, CURLOPT_URL, req.url.c_str());

    // 设置方法
    curl_easy_setopt(curl, CURLOPT_CUSTOMREQUEST, req.method.c_str());

    // 设置超时
    curl_easy_setopt(curl, CURLOPT_TIMEOUT, req.timeout);

    // 设置跟随重定向
    if (req.follow_redirects) {
        curl_easy_setopt(curl, CURLOPT_FOLLOWLOCATION, 1L);
        curl_easy_setopt(curl, CURLOPT_MAXREDIRS, req.max_redirects);
    }

    // 设置请求头
    struct curl_slist* headers = nullptr;
    for (const auto& [key, value] : req.headers) {
        std::string header = key + ": " + value;
        headers = curl_slist_append(headers, header.c_str());
    }

    // 添加默认头
    for (const auto& [key, value] : default_headers_) {
        std::string header = key + ": " + value;
        headers = curl_slist_append(headers, header.c_str());
    }

    curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headers);

    // 设置请求体
    if (!req.body.empty()) {
        curl_easy_setopt(curl, CURLOPT_POSTFIELDS, req.body.c_str());
        curl_easy_setopt(curl, CURLOPT_POSTFIELDSIZE, req.body.size());
    }

    // 设置响应处理
    std::string response_body;
    curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, write_callback);
    curl_easy_setopt(curl, CURLOPT_WRITEDATA, &response_body);

    // 设置响应头处理
    std::string response_headers;
    curl_easy_setopt(curl, CURLOPT_HEADERFUNCTION, header_callback);
    curl_easy_setopt(curl, CURLOPT_HEADERDATA, &response_headers);

    // 执行请求
    CURLcode res = curl_easy_perform(curl);

    if (res != CURLE_OK) {
        curl_slist_free_all(headers);
        curl_easy_cleanup(curl);
        throw std::runtime_error("curl_easy_perform() failed: " + std::string(curl_easy_strerror(res)));
    }

    // 获取响应码
    long status_code;
    curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &status_code);
    response.status_code = static_cast<int>(status_code);

    // 解析响应头
    response.headers = parse_headers(response_headers);

    // 设置响应体
    response.body = response_body;

    // 清理
    curl_slist_free_all(headers);
    curl_easy_cleanup(curl);

    co_return response;
}

size_t write_callback(void* contents, size_t size, size_t nmemb, void* userp) {
    size_t total_size = size * nmemb;
    std::string* response = static_cast<std::string*>(userp);
    response->append(static_cast<char*>(contents), total_size);
    return total_size;
}

size_t header_callback(char* buffer, size_t size, size_t nitems, void* userdata) {
    size_t total_size = size * nitems;
    std::string* headers = static_cast<std::string*>(userdata);
    headers->append(buffer, total_size);
    return total_size;
}
```

### 5.2 流式响应处理

```cpp
Task<StreamResponse> HttpClient::request_stream(const HttpRequest& req) {
    StreamResponse response;

    CURL* curl = curl_easy_init();
    if (!curl) {
        throw std::runtime_error("Failed to initialize curl");
    }

    curl_easy_setopt(curl, CURLOPT_URL, req.url.c_str());
    curl_easy_setopt(curl, CURLOPT_CUSTOMREQUEST, req.method.c_str());
    curl_easy_setopt(curl, CURLOPT_TIMEOUT, req.timeout);

    // 设置流式响应
    curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, nullptr);
    curl_easy_setopt(curl, CURLOPT_WRITEDATA, nullptr);

    // 创建异步生成器
    auto generator = co_await create_stream_generator(curl, req);

    // 获取响应头
    long status_code;
    curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &status_code);
    response.status_code = static_cast<int>(status_code);

    response.body_stream = std::move(generator);

    co_return response;
}
```

## 6. 测试策略

### 6.1 单元测试

**测试覆盖**:
- HTTP GET/POST/PUT/DELETE
- 请求头处理
- 响应解析
- 超时处理
- 错误处理

**测试示例**:

```cpp
TEST_CASE("HttpClient::get", "[network][http]") {
    HttpClient client;

    auto response = co_await client.get("https://httpbin.org/get");

    REQUIRE(response.is_success());
    REQUIRE(response.status_code == 200);

    auto json = nlohmann::json::parse(response.body);
    REQUIRE(json.contains("url"));
}

TEST_CASE("HttpClient::post", "[network][http]") {
    HttpClient client;

    nlohmann::json body = {
        {"key", "value"},
        {"number", 42}
    };

    auto response = co_await client.post("https://httpbin.org/post", body);

    REQUIRE(response.is_success());
    REQUIRE(response.status_code == 200);

    auto json = nlohmann::json::parse(response.body);
    REQUIRE(json["json"]["key"] == "value");
    REQUIRE(json["json"]["number"] == 42);
}
```

### 6.2 集成测试

测试与真实API的交互。

## 7. 使用示例

### 7.1 HTTP请求

```cpp
#include "turbot/network/http/http_client.hpp"

using namespace turbot::network::http;

// 创建客户端
HttpClient client;

// GET请求
auto response = co_await client.get("https://api.openai.com/v1/models", {
    {"Authorization", "Bearer sk-xxx"}
});

if (response.is_success()) {
    auto json = nlohmann::json::parse(response.body);
    TURBOT_LOG_INFO("Models: {}", json.dump());
}

// POST请求
nlohmann::json body = {
    {"model", "gpt-4"},
    {"messages", nlohmann::json::array({
        {{"role", "user"}, {"content", "Hello"}}
    })}
};

auto response = co_await client.post("https://api.openai.com/v1/chat/completions", body, {
    {"Authorization", "Bearer sk-xxx"},
    {"Content-Type", "application/json"}
});
```

### 7.2 流式响应

```cpp
// 流式请求
HttpRequest req;
req.method = "POST";
req.url = "https://api.openai.com/v1/chat/completions";
req.headers = {
    {"Authorization", "Bearer sk-xxx"},
    {"Content-Type", "application/json"}
};
req.body = body.dump();

auto stream = co_await client.request_stream(req);

if (stream.is_success()) {
    for co_await (const auto& chunk : stream.body_stream) {
        // 处理SSE数据
        TURBOT_LOG_INFO("Chunk: {}", chunk);
    }
}
```

## 8. 验收标准

### 8.1 功能验收

- [ ] HTTP基本操作正常
- [ ] 流式响应正常
- [ ] WebSocket连接正常
- [ ] 所有测试通过

### 8.2 性能验收

- [ ] HTTP请求延迟 < 100ms（本地）
- [ ] 并发请求支持 >= 100个

### 8.3 代码质量验收

- [ ] 通过 clang-tidy 静态分析
- [ ] 单元测试覆盖率 >= 80%

## 9. 时间估算

| 任务 | 估算时间 |
|------|---------|
| HTTP客户端实现 | 3-4天 |
| 流式响应处理 | 2-3天 |
| WebSocket客户端 | 2-3天 |
| 异步IO层 | 1-2天 |
| 单元测试 | 2-3天 |
| 集成测试 | 1-2天 |
| 文档编写 | 1天 |
| **总计** | **12-18天** |

## 10. 下一步

完成本计划后，进入**1.4 - 核心数据模型实现计划**，实现Message和Part系统。

## 11. 参考文档

- libcurl文档: https://curl.se/libcurl/c/
- Asio文档: https://think-async.com/Asio/
- WebSocket协议: https://tools.ietf.org/html/rfc6455