# Turbot-AI 多 Agent 协作架构技术实现方案

## 1. 项目概述

### 1.1 背景

基于对 OpenCode 项目架构的深入分析，Turbot-AI 项目旨在使用 C++23 实现一个类似的多 Agent 协作 AI 开发助手框架。本项目将借鉴 OpenCode 的核心设计理念，结合 C++ 的高性能特性，构建一个高效、安全、可扩展的 AI 编码助手系统。

### 1.2 设计目标

- **高性能**: 利用 C++ 的性能优势，提供低延迟的 AI 交互体验
- **模块化**: 清晰的模块边界，易于维护和扩展
- **类型安全**: 利用 C++23 的强类型系统和概念约束
- **跨平台**: 支持主流操作系统（Linux, macOS, Windows）
- **安全性**: 完善的权限系统和安全机制
- **可观测性**: 内置日志、监控和追踪能力

### 1.3 核心特性

- 多 Agent 协作机制
- 流式 AI 响应处理
- 智能会话压缩
- 灵活的工具系统
- 强大的权限控制
- 事件驱动架构
- 持久化存储

## 2. 整体架构设计

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    CLI 入口层                              │
│              (turbot-cli)                                 │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                    命令执行层                              │
│         (Command System)                                  │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                   核心业务层                               │
│  ┌──────────┬──────────┬──────────┬──────────┐           │
│  │ Session  │  Agent   │  Tool    │ Provider │           │
│  │  会话管理 │  代理    │  工具系统 │  提供商  │           │
│  └──────────┴──────────┴──────────┴──────────┘           │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                   数据持久层                               │
│              (Storage Layer)                              │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                   基础设施层                               │
│  ┌──────────┬──────────┬──────────┬──────────┐           │
│  │   Bus    │  Config  │  Auth    │  Plugin  │           │
│  │  事件总线 │  配置管理 │  认证系统 │  插件系统 │           │
│  └──────────┴──────────┴──────────┴──────────┘           │
└─────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
turbot-ai/
├── libs/
│   ├── core/              # 核心库
│   │   ├── session/       # 会话管理
│   │   ├── agent/         # 代理系统
│   │   ├── tool/          # 工具系统
│   │   ├── provider/      # AI提供商
│   │   ├── message/       # 消息系统
│   │   ├── permission/    # 权限系统
│   │   └── event/         # 事件系统
│   ├── network/           # 网络库
│   │   ├── http/          # HTTP客户端
│   │   └── websocket/     # WebSocket支持
│   ├── storage/           # 存储库
│   │   ├── database/      # 数据库抽象
│   │   ├── sqlite/        # SQLite实现
│   │   └── postgresql/    # PostgreSQL实现
│   └── utils/             # 工具库
│       ├── json/          # JSON处理
│       ├── string/        # 字符串处理
│       └── crypto/        # 加密工具
├── apps/
│   ├── turbot-cli/        # CLI应用
│   └── turbot-server/     # 服务器应用
├── tools/                 # 内置工具实现
│   ├── read/
│   ├── write/
│   ├── edit/
│   └── ...
└── agents/                # 内置代理实现
    ├── build/
    ├── plan/
    ├── explore/
    └── ...
```

## 3. 核心组件设计

### 3.1 Session（会话管理）

#### 3.1.1 数据模型

```cpp
// libs/core/session/session.hpp
namespace turbot::core::session {

struct SessionInfo {
    std::string id;
    std::string project_id;
    std::optional<std::string> workspace_id;
    std::optional<std::string> parent_id;
    std::string slug;
    std::string directory;
    std::string title;
    std::string version;
    std::optional<std::string> share_url;
    std::optional<int64_t> summary_additions;
    std::optional<int64_t> summary_deletions;
    std::optional<int32_t> summary_files;
    std::optional<nlohmann::json> summary_diffs;
    std::optional<nlohmann::json> revert;
    std::optional<nlohmann::json> permission;
    int64_t time_created;
    int64_t time_updated;
    std::optional<int64_t> time_compacting;
    std::optional<int64_t> time_archived;
};

class Session {
public:
    // 创建新会话
    static Task<Session> create(const CreateParams& params);

    // Fork会话
    static Task<Session> fork(const ForkParams& params);

    // 获取会话
    static Task<std::optional<Session>> get(const std::string& id);

    // 更新会话
    Task<void> update(const UpdateParams& params);

    // 设置标题
    Task<void> set_title(const std::string& title);

    // 设置权限
    Task<void> set_permission(const nlohmann::json& permission);

    // 设置回滚点
    Task<void> set_revert(const nlohmann::json& revert);

    // 获取消息流
    Task<std::vector<message::Message>> messages(int limit = 50, int offset = 0);

    // 压缩会话
    Task<void> compact();

    // 归档会话
    Task<void> archive();

private:
    SessionInfo info_;
    std::shared_ptr<storage::Database> db_;
};

} // namespace turbot::core::session
```

#### 3.1.2 会话生命周期

```cpp
// 会话状态机
enum class SessionState {
    Created,
    Active,
    Busy,
    Compacting,
    Archived
};

class SessionStateMachine {
public:
    bool can_transition(SessionState from, SessionState to) const;
    void transition(SessionState to);
    SessionState current() const;

private:
    SessionState state_ = SessionState::Created;
    std::mutex mutex_;
};
```

### 3.2 Agent（代理系统）

#### 3.2.1 代理类型定义

```cpp
// libs/core/agent/agent.hpp
namespace turbot::core::agent {

enum class AgentMode {
    Primary,    // 主要代理，用户可直接选择
    Subagent,   // 子代理，只能通过task工具调用
    All         // 混合模式
};

struct AgentInfo {
    std::string name;
    std::optional<std::string> description;
    AgentMode mode;
    bool native = false;
    bool hidden = false;
    std::optional<double> top_p;
    std::optional<double> temperature;
    std::optional<std::string> color;
    nlohmann::json permission;  // 权限规则集
    std::optional<provider::ModelConfig> model;
    std::optional<std::string> variant;
    std::optional<std::string> prompt;
    nlohmann::json options;
    std::optional<int32_t> steps;
};

class Agent {
public:
    // 注册代理
    static void register_agent(const AgentInfo& info);

    // 获取代理
    static Task<std::optional<Agent>> get(const std::string& name);

    // 列出所有代理
    static Task<std::vector<Agent>> list();

    // 执行代理任务
    Task<nlohmann::json> execute(const ExecuteParams& params);

    // 检查权限
    bool check_permission(const std::string& permission,
                         const std::string& pattern) const;

private:
    AgentInfo info_;
    std::shared_ptr<provider::Provider> provider_;
};

} // namespace turbot::core::agent
```

#### 3.2.2 内置代理实现

```cpp
// libs/core/agent/builtin/build_agent.cpp
namespace turbot::core::agent::builtin {

class BuildAgent : public Agent {
public:
    BuildAgent() {
        info_.name = "build";
        info_.description = "The default agent. Executes tools based on configured permissions.";
        info_.mode = AgentMode::Primary;
        info_.permission = build_default_permission();
    }

private:
    nlohmann::json build_default_permission() {
        nlohmann::json permission = nlohmann::json::array();
        permission.push_back({
            {"permission", "*"},
            {"pattern", "*"},
            {"action", "allow"}
        });
        permission.push_back({
            {"permission", "doom_loop"},
            {"pattern", "*"},
            {"action", "ask"}
        });
        return permission;
    }
};

} // namespace turbot::core::agent::builtin
```

### 3.3 Tool（工具系统）

#### 3.3.1 工具接口定义

```cpp
// libs/core/tool/tool.hpp
namespace turbot::core::tool {

struct ToolContext {
    std::string session_id;
    std::string message_id;
    std::string agent;
    std::optional<std::string> call_id;
    std::function<void(const nlohmann::json&)> metadata;
    std::function<Task<void>(const permission::PermissionRequest&)> ask;
    std::vector<message::Message> messages;
    std::shared_ptr<std::atomic<bool>> abort_flag;
};

struct ToolResult {
    std::string title;
    std::string output;
    nlohmann::json metadata;
    std::vector<message::FilePart> attachments;
};

class Tool {
public:
    virtual ~Tool() = default;

    // 工具名称
    virtual std::string name() const = 0;

    // 工具描述
    virtual std::string description() const = 0;

    // 输入参数schema (JSON Schema)
    virtual nlohmann::json input_schema() const = 0;

    // 执行工具
    virtual Task<ToolResult> execute(const nlohmann::json& input,
                                     ToolContext& ctx) = 0;

    // 验证输入
    virtual bool validate_input(const nlohmann::json& input) const;

    // 格式化验证错误
    virtual std::string format_validation_error(const std::string& error) const;
};

} // namespace turbot::core::tool
```

#### 3.3.2 工具注册表

```cpp
// libs/core/tool/registry.hpp
namespace turbot::core::tool {

class ToolRegistry {
public:
    // 单例访问
    static ToolRegistry& instance();

    // 注册工具
    void register_tool(std::unique_ptr<Tool> tool);

    // 获取工具
    std::shared_ptr<Tool> get(const std::string& name) const;

    // 列出所有工具
    std::vector<std::shared_ptr<Tool>> list() const;

    // 根据代理过滤工具
    std::vector<std::shared_ptr<Tool>> filter(const agent::Agent& agent) const;

private:
    ToolRegistry() = default;
    std::unordered_map<std::string, std::shared_ptr<Tool>> tools_;
    mutable std::shared_mutex mutex_;
};

} // namespace turbot::core::tool
```

### 3.4 Provider（AI 提供商）

#### 3.4.1 提供商接口

```cpp
// libs/core/provider/provider.hpp
namespace turbot::core::provider {

struct ModelConfig {
    std::string model_id;
    std::string provider_id;
};

struct ModelCapabilities {
    bool temperature = false;
    bool reasoning = false;
    bool attachment = false;
    bool tool_call = false;
    struct {
        bool text = false;
        bool audio = false;
        bool image = false;
        bool video = false;
        bool pdf = false;
    } input;
    struct {
        bool text = false;
        bool audio = false;
        bool image = false;
        bool video = false;
        bool pdf = false;
    } output;
};

struct ModelInfo {
    std::string id;
    std::string provider_id;
    std::string name;
    std::optional<std::string> family;
    ModelCapabilities capabilities;
    struct {
        double input;
        double output;
        struct {
            double read;
            double write;
        } cache;
    } cost;
    struct {
        int32_t context;
        std::optional<int32_t> input;
        std::optional<int32_t> output;
    } limit;
    std::string status;
    nlohmann::json options;
    nlohmann::json headers;
};

struct ChatMessage {
    enum class Role { System, User, Assistant, Tool };
    Role role;
    std::string content;
    std::optional<std::string> tool_call_id;
    std::optional<nlohmann::json> tool_calls;
};

struct ChatStreamEvent {
    enum class Type {
        TextDelta,
        ToolCall,
        Finish,
        Error
    };
    Type type;
    std::string content;
    nlohmann::json data;
};

class Provider {
public:
    virtual ~Provider() = default;

    // 获取模型信息
    virtual Task<ModelInfo> get_model(const std::string& model_id) = 0;

    // 聊天请求（流式）
    virtual AsyncGenerator<ChatStreamEvent> chat_stream(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options = {}) = 0;

    // 聊天请求（非流式）
    virtual Task<ChatMessage> chat(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options = {}) = 0;

    // 验证认证
    virtual Task<bool> authenticate() = 0;

    // 提供商名称
    virtual std::string name() const = 0;
};

} // namespace turbot::core::provider
```

#### 3.4.2 OpenAI 提供商实现

```cpp
// libs/core/provider/openai/openai_provider.cpp
namespace turbot::core::provider::openai {

class OpenAIProvider : public Provider {
public:
    OpenAIProvider(const std::string& api_key)
        : api_key_(api_key),
          client_("https://api.openai.com/v1") {}

    Task<ModelInfo> get_model(const std::string& model_id) override {
        co_return ModelInfo{
            .id = model_id,
            .provider_id = "openai",
            .name = model_id,
            .capabilities = {/* ... */},
            .cost = {/* ... */},
            .limit = {/* ... */}
        };
    }

    AsyncGenerator<ChatStreamEvent> chat_stream(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options) override {

        nlohmann::json request = {
            {"model", config.model_id},
            {"messages", messages_to_json(messages)},
            {"stream", true}
        };

        auto response = co_await client_.post("/chat/completions", request, {
            {"Authorization", "Bearer " + api_key_}
        });

        // 流式处理响应
        for (const auto& chunk : response.body()) {
            auto event = parse_sse_chunk(chunk);
            co_yield event;
        }
    }

private:
    std::string api_key_;
    network::HttpClient client_;
};

} // namespace turbot::core::provider::openai
```

### 3.5 Message（消息系统）

#### 3.5.1 消息数据模型

```cpp
// libs/core/message/message.hpp
namespace turbot::core::message {

struct Part {
    std::string id;
    std::string message_id;
    std::string session_id;
    int64_t time_created;
    int64_t time_updated;

    enum class Type {
        Text,
        Tool,
        Reasoning,
        File,
        Subtask,
        StepStart,
        StepFinish,
        Snapshot,
        Patch,
        Agent,
        Retry,
        Compaction
    } type;

    nlohmann::json data;
};

struct MessageInfo {
    std::string id;
    std::string session_id;
    enum class Role { User, Assistant, System } role;
    int64_t time_created;
    int64_t time_updated;
    std::optional<std::string> parent_id;
    std::string agent;
    provider::ModelConfig model;
    std::optional<std::string> system;
    std::optional<nlohmann::json> tools;
    std::optional<std::string> variant;
    std::optional<nlohmann::json> error;
    std::optional<std::string> finish;
    double cost;
    TokenUsage tokens;
    std::optional<bool> summary;
    std::optional<nlohmann::json> structured;
};

struct Message {
    MessageInfo info;
    std::vector<Part> parts;
};

struct TokenUsage {
    int64_t input;
    int64_t output;
    int64_t reasoning;
    struct {
        int64_t read;
        int64_t write;
    } cache;
};

} // namespace turbot::core::message
```

### 3.6 Permission（权限系统）

#### 3.6.1 权限规则

```cpp
// libs/core/permission/permission.hpp
namespace turbot::core::permission {

enum class PermissionAction {
    Allow,
    Deny,
    Ask
};

struct PermissionRule {
    std::string permission;  // 权限类型
    std::string pattern;     // 匹配模式
    PermissionAction action;
};

using Ruleset = std::vector<PermissionRule>;

struct PermissionRequest {
    std::string permission;
    std::vector<std::string> patterns;
    std::optional<std::string> tool;
    nlohmann::json metadata;
};

struct PermissionReply {
    enum class Type { Once, Always, Reject } type;
    std::optional<std::string> message;
};

class PermissionSystem {
public:
    // 评估权限
    PermissionAction evaluate(const std::string& permission,
                             const std::string& pattern,
                             const Ruleset& ruleset) const;

    // 合并规则集
    static Ruleset merge(const std::vector<Ruleset>& rulesets);

    // 通配符匹配
    static bool wildcard_match(const std::string& pattern,
                               const std::string& text);

    // 请求权限
    Task<PermissionReply> ask(const PermissionRequest& request);

    // 回复权限请求
    Task<void> reply(const std::string& request_id,
                     const PermissionReply& reply);

private:
    std::unordered_map<std::string, std::promise<PermissionReply>> pending_;
    std::mutex mutex_;
};

} // namespace turbot::core::permission
```

### 3.7 Event（事件系统）

#### 3.7.1 事件总线

```cpp
// libs/core/event/bus.hpp
namespace turbot::core::event {

template<typename T>
struct Event {
    std::string name;
    T data;
    int64_t timestamp;
};

template<typename T>
using EventHandler = std::function<Task<void>(const Event<T>&)>;

class EventBus {
public:
    // 单例访问
    static EventBus& instance();

    // 发布事件
    template<typename T>
    Task<void> publish(const std::string& name, T data) {
        Event<T> event{
            .name = name,
            .data = std::move(data),
            .timestamp = std::chrono::system_clock::now().time_since_epoch().count()
        };

        std::shared_lock lock(mutex_);
        auto it = handlers_.find(name);
        if (it != handlers_.end()) {
            for (auto& handler : it->second) {
                co_await handler(event);
            }
        }
    }

    // 订阅事件
    template<typename T>
    std::string subscribe(const std::string& name, EventHandler<T> handler) {
        std::string id = generate_id();
        std::unique_lock lock(mutex_);
        handlers_[name].push_back({
            .id = id,
            .handler = [handler](const std::any& event) -> Task<void> {
                co_await handler(std::any_cast<const Event<T>&>(event));
            }
        });
        return id;
    }

    // 取消订阅
    void unsubscribe(const std::string& name, const std::string& handler_id);

private:
    struct HandlerEntry {
        std::string id;
        std::function<Task<void>(const std::any&)> handler;
    };

    EventBus() = default;
    std::unordered_map<std::string, std::vector<HandlerEntry>> handlers_;
    mutable std::shared_mutex mutex_;
};

} // namespace turbot::core::event
```

## 4. 多 Agent 协作机制

### 4.1 Task 工具实现

```cpp
// tools/task/task_tool.hpp
namespace turbot::tools::task {

struct TaskParams {
    std::string prompt;
    std::string description;
    std::string subagent_type;
    std::optional<std::string> task_id;
    std::optional<std::string> command;
};

class TaskTool : public core::tool::Tool {
public:
    std::string name() const override { return "task"; }

    std::string description() const override {
        return "Execute a subtask using a subagent";
    }

    nlohmann::json input_schema() const override {
        return R"({
            "type": "object",
            "properties": {
                "prompt": {"type": "string"},
                "description": {"type": "string"},
                "subagent_type": {"type": "string"},
                "task_id": {"type": "string"},
                "command": {"type": "string"}
            },
            "required": ["prompt", "description", "subagent_type"]
        })"_json;
    }

    Task<ToolResult> execute(const nlohmann::json& input,
                             ToolContext& ctx) override {
        TaskParams params = input.get<TaskParams>();

        // 权限检查
        if (!ctx.extra.contains("bypassAgentCheck")) {
            co_await ctx.ask({
                .permission = "task",
                .patterns = {params.subagent_type},
                .always = {"*"},
                .metadata = {
                    {"description", params.description},
                    {"subagent_type", params.subagent_type}
                }
            });
        }

        // 获取子代理
        auto agent = co_await agent::Agent::get(params.subagent_type);
        if (!agent) {
            throw std::runtime_error("Agent not found: " + params.subagent_type);
        }

        // 创建或恢复子会话
        session::Session sub_session;
        if (params.task_id) {
            auto existing = co_await session::Session::get(*params.task_id);
            if (existing) {
                sub_session = std::move(*existing);
            }
        }

        if (!sub_session) {
            // 创建新子会话
            sub_session = co_await session::Session::create({
                .parent_id = ctx.session_id,
                .title = params.description + " (@" + agent->name() + " subagent)",
                .permission = build_subagent_permission(*agent)
            });
        }

        // 在子会话中执行任务
        auto result = co_await execute_in_subsession(sub_session, *agent, params, ctx);

        co_return ToolResult{
            .title = params.description,
            .output = format_task_result(result, sub_session.id()),
            .metadata = {
                {"session_id", sub_session.id()},
                {"subagent", params.subagent_type}
            }
        };
    }

private:
    permission::Ruleset build_subagent_permission(const agent::Agent& agent) {
        // 限制子会话权限
        return {
            {{"permission", "todowrite"}, {"pattern", "*"}, {"action", "deny"}},
            {{"permission", "todoread"}, {"pattern", "*"}, {"action", "deny"}},
            {{"permission", "task"}, {"pattern", "*"}, {"action", "deny"}}
        };
    }

    Task<std::string> execute_in_subsession(
        session::Session& session,
        const agent::Agent& agent,
        const TaskParams& params,
        ToolContext& ctx) {
        // 创建用户消息
        auto user_msg = co_await message::Message::create({
            .session_id = session.id(),
            .role = message::Role::User,
            .content = params.prompt
        });

        // 执行会话主循环
        auto assistant_msg = co_await session::Session::prompt({
            .session_id = session.id(),
            .message_id = user_msg.id(),
            .agent = agent.name()
        });

        co_return assistant_msg.to_text();
    }

    std::string format_task_result(const std::string& result, const std::string& session_id) {
        return "task_id: " + session_id + "\n<task_result>\n" + result + "\n</task_result>";
    }
};

} // namespace turbot::tools::task
```

### 4.2 会话主循环

```cpp
// libs/core/session/session_loop.cpp
namespace turbot::core::session {

class SessionLoop {
public:
    SessionLoop(const std::string& session_id) : session_id_(session_id) {}

    Task<void> run() {
        int step = 0;
        while (true) {
            // 获取消息历史
            auto messages = co_await Message::stream(session_id_);

            // 找到最后一条用户消息和助手消息
            auto [last_user, last_assistant] = find_last_messages(messages);

            // 收集待处理任务
            auto pending_tasks = collect_pending_tasks(messages);

            // 处理路径选择
            if (!pending_tasks.empty()) {
                if (auto* task = std::get_if<SubtaskPart>(&pending_tasks.back())) {
                    co_await handle_subtask(*task, last_user);
                    continue;
                }
                if (auto* task = std::get_if<CompactionPart>(&pending_tasks.back())) {
                    co_await handle_compaction(*task);
                    continue;
                }
            }

            // 检查是否需要压缩
            if (should_compact(messages)) {
                co_await create_compaction_task();
                continue;
            }

            // 正常LLM处理
            auto result = co_await process_llm_request(messages);
            if (result == LoopResult::Stop) {
                break;
            }

            step++;
        }
    }

private:
    std::string session_id_;

    enum class LoopResult {
        Continue,
        Stop,
        Compact
    };

    Task<void> handle_subtask(const SubtaskPart& task, const Message* last_user) {
        // 创建助手消息
        auto assistant_msg = co_await Message::create({
            .session_id = session_id_,
            .role = Role::Assistant,
            .parent_id = last_user->id(),
            .agent = task.agent
        });

        // 执行Task工具
        TaskTool task_tool;
        auto result = co_await task_tool.execute(task.params, {
            .session_id = session_id_,
            .message_id = assistant_msg.id(),
            .agent = task.agent
        });

        // 更新消息
        assistant_msg.add_part({
            .type = PartType::Tool,
            .tool = "task",
            .state = ToolState::Completed,
            .output = result.output
        });

        co_await assistant_msg.save();
    }

    Task<LoopResult> process_llm_request(const std::vector<Message>& messages) {
        // 获取代理
        auto agent = co_await Agent::get(get_agent_name(messages));

        // 获取模型
        auto model = co_await Provider::get_default_model();

        // 获取可用工具
        auto tools = co_await ToolRegistry::instance().filter(*agent);

        // 构建系统提示词
        auto system = build_system_prompt(*agent);

        // 调用LLM
        auto stream = co_await model->chat_stream(
            to_model_messages(messages, system),
            {model->id(), model->provider_id()}
        );

        // 处理流式响应
        auto assistant_msg = co_await Message::create({
            .session_id = session_id_,
            .role = Role::Assistant
        });

        bool has_tool_calls = false;
        for (auto event : stream) {
            switch (event.type) {
                case ChatStreamEvent::Type::TextDelta:
                    assistant_msg.append_text(event.content);
                    break;
                case ChatStreamEvent::Type::ToolCall:
                    has_tool_calls = true;
                    co_await handle_tool_call(event.data, assistant_msg);
                    break;
                case ChatStreamEvent::Type::Finish:
                    assistant_msg.set_finish(event.content);
                    break;
                case ChatStreamEvent::Type::Error:
                    assistant_msg.set_error(event.data);
                    break;
            }
        }

        co_await assistant_msg.save();

        return has_tool_calls ? LoopResult::Continue : LoopResult::Stop;
    }
};

} // namespace turbot::core::session
```

## 5. 数据持久层设计

### 5.1 数据库抽象层

```cpp
// libs/storage/database/database.hpp
namespace turbot::storage {

struct QueryResult {
    std::vector<nlohmann::json> rows;
    int64_t affected_rows;
};

class Database {
public:
    virtual ~Database() = default;

    // 执行查询
    virtual Task<QueryResult> execute(const std::string& sql,
                                     const std::vector<nlohmann::json>& params = {}) = 0;

    // 开始事务
    virtual Task<std::shared_ptr<Transaction>> begin_transaction() = 0;

    // 迁移
    virtual Task<void> migrate(const std::string& migration_name,
                               const std::string& sql) = 0;

    // 健康检查
    virtual Task<bool> health_check() = 0;
};

class Transaction {
public:
    virtual ~Transaction() = default;

    virtual Task<void> commit() = 0;
    virtual Task<void> rollback() = 0;

    virtual Task<QueryResult> execute(const std::string& sql,
                                     const std::vector<nlohmann::json>& params = {}) = 0;
};

} // namespace turbot::storage
```

### 5.2 SQLite 实现

```cpp
// libs/storage/sqlite/sqlite_database.cpp
namespace turbot::storage::sqlite {

class SQLiteDatabase : public Database {
public:
    SQLiteDatabase(const std::string& path) {
        int rc = sqlite3_open(path.c_str(), &db_);
        if (rc != SQLITE_OK) {
            throw std::runtime_error("Cannot open database: " + std::string(sqlite3_errmsg(db_)));
        }
    }

    ~SQLiteDatabase() override {
        if (db_) {
            sqlite3_close(db_);
        }
    }

    Task<QueryResult> execute(const std::string& sql,
                             const std::vector<nlohmann::json>& params) override {
        sqlite3_stmt* stmt = nullptr;
        int rc = sqlite3_prepare_v2(db_, sql.c_str(), -1, &stmt, nullptr);
        if (rc != SQLITE_OK) {
            throw std::runtime_error("Prepare failed: " + std::string(sqlite3_errmsg(db_)));
        }

        // 绑定参数
        for (size_t i = 0; i < params.size(); i++) {
            bind_param(stmt, i + 1, params[i]);
        }

        // 执行查询
        QueryResult result;
        while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
            result.rows.push_back(row_to_json(stmt));
        }

        result.affected_rows = sqlite3_changes(db_);
        sqlite3_finalize(stmt);

        if (rc != SQLITE_DONE) {
            throw std::runtime_error("Step failed: " + std::string(sqlite3_errmsg(db_)));
        }

        co_return result;
    }

private:
    sqlite3* db_ = nullptr;

    void bind_param(sqlite3_stmt* stmt, int index, const nlohmann::json& value) {
        if (value.is_null()) {
            sqlite3_bind_null(stmt, index);
        } else if (value.is_string()) {
            sqlite3_bind_text(stmt, index, value.get<std::string>().c_str(), -1, SQLITE_TRANSIENT);
        } else if (value.is_number_integer()) {
            sqlite3_bind_int64(stmt, index, value.get<int64_t>());
        } else if (value.is_number_float()) {
            sqlite3_bind_double(stmt, index, value.get<double>());
        } else if (value.is_boolean()) {
            sqlite3_bind_int(stmt, index, value.get<bool>() ? 1 : 0);
        } else {
            sqlite3_bind_text(stmt, index, value.dump().c_str(), -1, SQLITE_TRANSIENT);
        }
    }

    nlohmann::json row_to_json(sqlite3_stmt* stmt) {
        nlohmann::json row;
        int count = sqlite3_column_count(stmt);
        for (int i = 0; i < count; i++) {
            const char* name = sqlite3_column_name(stmt, i);
            switch (sqlite3_column_type(stmt, i)) {
                case SQLITE_TEXT:
                    row[name] = reinterpret_cast<const char*>(sqlite3_column_text(stmt, i));
                    break;
                case SQLITE_INTEGER:
                    row[name] = sqlite3_column_int64(stmt, i);
                    break;
                case SQLITE_FLOAT:
                    row[name] = sqlite3_column_double(stmt, i);
                    break;
                case SQLITE_NULL:
                    row[name] = nullptr;
                    break;
            }
        }
        return row;
    }
};

} // namespace turbot::storage::sqlite
```

### 5.3 数据库迁移

```cpp
// libs/storage/migration/migration.hpp
namespace turbot::storage::migration {

class Migration {
public:
    virtual std::string name() const = 0;
    virtual std::string up() const = 0;
    virtual std::string down() const = 0;
};

class MigrationRunner {
public:
    MigrationRunner(std::shared_ptr<Database> db) : db_(std::move(db)) {}

    Task<void> run() {
        // 创建迁移表
        co_await create_migration_table();

        // 获取已执行的迁移
        auto executed = co_await get_executed_migrations();

        // 执行新迁移
        for (const auto& migration : migrations_) {
            if (executed.contains(migration->name())) {
                continue;
            }

            TURBOT_LOG_INFO("Running migration: {}", migration->name());

            co_await db_->execute(migration->up());
            co_await record_migration(migration->name());
        }
    }

    void add_migration(std::unique_ptr<Migration> migration) {
        migrations_.push_back(std::move(migration));
    }

private:
    std::shared_ptr<Database> db_;
    std::vector<std::unique_ptr<Migration>> migrations_;

    Task<void> create_migration_table() {
        co_await db_->execute(R"(
            CREATE TABLE IF NOT EXISTS schema_migrations (
                name TEXT PRIMARY KEY,
                executed_at INTEGER NOT NULL
            )
        )");
    }

    Task<std::unordered_set<std::string>> get_executed_migrations() {
        auto result = co_await db_->execute("SELECT name FROM schema_migrations");
        std::unordered_set<std::string> migrations;
        for (const auto& row : result.rows) {
            migrations.insert(row["name"].get<std::string>());
        }
        co_return migrations;
    }

    Task<void> record_migration(const std::string& name) {
        co_await db_->execute(
            "INSERT INTO schema_migrations (name, executed_at) VALUES (?, ?)",
            {name, std::chrono::system_clock::now().time_since_epoch().count()}
        );
    }
};

} // namespace turbot::storage::migration
```

## 6. 技术栈选择

### 6.1 核心依赖

| 类别         | 库             | 版本          | 用途        |
| ------------ | -------------- | ------------- | ----------- |
| **编译器**   | GCC/Clang/MSVC | 14+/18+/2022+ | C++23 支持  |
| **构建系统** | CMake          | 3.28+         | 构建配置    |
| **包管理**   | Conan          | 2.x           | 依赖管理    |
| **网络**     | Asio           | 1.28+         | 异步网络 IO |
| **JSON**     | nlohmann/json  | 3.11+         | JSON 处理   |
| **日志**     | spdlog         | 1.13+         | 日志系统    |
| **测试**     | Catch2         | 3.5+          | 单元测试    |
| **加密**     | OpenSSL        | 3.x           | 加密和 SSL  |
| **压缩**     | zlib           | 1.3+          | 数据压缩    |
| **HTTP**     | libcurl        | 8.x           | HTTP 客户端 |
| **数据库**   | SQLite         | 3.40+         | 本地存储    |

### 6.2 可选依赖

| 类别          | 库          | 版本 | 用途           |
| ------------- | ----------- | ---- | -------------- |
| **数据库**    | PostgreSQL  | 15+  | 远程存储       |
| **Web 框架**  | Drogon      | 1.9+ | HTTP 服务器    |
| **WebSocket** | uWebSockets | 20+  | WebSocket 支持 |
| **ORM**       | SQLiteORM   | 1.8+ | SQLite ORM     |

### 6.3 C++23 特性应用

- **协程** (`co_await`, `co_return`)：异步编程
- **模块** (`module`, `import`)：代码组织
- **概念** (`concept`)：类型约束
- **Ranges**：算法和容器操作
- **std::format**：格式化输出
- **std::generator**：生成器支持
- **std::expected**：错误处理
- **std::flat_map/flat_set**：优化容器

## 7. 实现路线图

> **最后更新**: 2026-03-08
> **总体进度**: 80% (32/40 任务完成)

### 7.1 第一阶段：基础架构 ✅ **已完成**

**目标**: 建立项目基础架构和核心基础设施

**任务**:

- [x] 项目初始化和构建配置
- [x] 实现事件总线系统 → `libs/core/src/event/event_bus.cpp`
- [x] 实现日志系统 → `libs/core/src/common/logger.hpp`
- [x] 实现配置管理系统 → `libs/core/src/config/`, `libs/utils/src/env_utils.cpp`
- [x] 实现数据库抽象层 → `libs/storage/src/database.cpp`
- [x] 实现 SQLite 存储后端 → `libs/storage/src/sqlite_database.cpp`
- [x] 实现迁移系统 → `libs/storage/src/migration.cpp`
- [x] 基础测试框架 → 20 个单元测试文件

**交付物**: ✅ 全部完成

- 完整的项目结构 (libs/, apps/, tests/)
- 事件总线实现 (`EventBus` 类)
- 日志系统实现 (`TURBOT_LOG_*` 宏)
- 数据库抽象层 (`Database`, `Transaction`)
- 单元测试覆盖 (464 test cases, 2124 assertions)

### 7.2 第二阶段：核心组件 ✅ **已完成**

**目标**: 实现 Session、Agent、Tool、Provider 等核心组件

**任务**:

- [x] 实现 Session 系统
  - [x] 数据模型 → `SessionInfo`, `Session` 类
  - [x] 会话生命周期管理 → `SessionStateMachine`
  - [x] 消息系统 → `libs/core/src/message/`
  - [x] Part 系统 → `Part` 结构体及多种类型
- [x] 实现 Agent 系统
  - [x] 代理注册表 → `Agent::register_agent()`
  - [x] 内置代理 → `build_agent.cpp`, `plan_agent.cpp`, `explore_agent.cpp`
  - [x] 代理配置 → `AgentInfo` 结构体
- [x] 实现 Tool 系统
  - [x] 工具接口 → `Tool` 抽象类
  - [x] 工具注册表 → `ToolRegistry`
  - [x] 内置工具 → **9 个工具**:
    - `read_file_tool.cpp`, `write_file_tool.cpp`, `edit_tool.cpp`
    - `bash_tool.cpp`, `grep_tool.cpp`, `glob_tool.cpp`, `list_tool.cpp`
    - `task_tool.cpp` (多 Agent 协作)
- [x] 实现 Provider 系统
  - [x] 提供商接口 → `Provider` 抽象类
  - [x] OpenAI 提供商 → `openai_provider.cpp`
  - [x] 模型管理 → `ModelInfo`, `ModelCapabilities`
  - [x] 额外提供商 → **5 个实现**:
    - `kimi_provider.cpp`, `zhipu_provider.cpp`
    - `bailian_provider.cpp`, `iflow_provider.cpp`
- [x] 实现 Permission 系统
  - [x] 权限规则 → `PermissionRule`, `Ruleset`
  - [x] 权限评估 → `PermissionSystem::evaluate()`
  - [x] 权限请求处理 → `PermissionRequest`, `PermissionReply`

**交付物**: ✅ 全部完成

### 7.3 第三阶段：多 Agent 协作 🔄 **进行中**

**目标**: 实现多 Agent 协作机制和会话主循环

**任务**:

- [x] 实现 Task 工具 → `task_tool.cpp`
  - [x] 子会话创建
  - [x] 权限控制
  - [x] 结果整合
- [x] 实现会话主循环 → `session_loop.cpp`
  - [x] 消息流处理
  - [x] 子任务处理
  - [x] 压缩检测
- [x] 实现会话压缩机制
  - [x] 压缩触发 → `needs_compaction()`
  - [x] 摘要生成 → `Session::compact()`
  - [ ] 历史清理 (待完善)
- [ ] 实现快照和回滚
  - [ ] 文件系统快照
  - [ ] 补丁生成
  - [ ] 回滚执行

**交付物**:

- ✅ 多 Agent 协作完整实现
- ✅ 会话主循环实现
- ⚠️ 会话压缩实现 (基础版本)
- ❌ 快照回滚实现

### 7.4 第四阶段：高级特性 🔄 **进行中**

**目标**: 实现流式处理、性能优化和安全机制

**任务**:

- [x] 实现流式 AI 响应处理 → `chat_stream()` 在所有 Provider 中实现
- [x] 实现成本追踪 → `TokenUsage`, `cost` 字段
- [~] 实现死循环检测 → 基础实现 (`needs_compaction()`)
- [ ] 实现缓存机制 (Prompt 缓存、模型信息缓存)
- [ ] 实现插件系统 (动态加载)
- [ ] 性能优化 (对象池、缓存优化)
- [ ] 安全加固 (沙箱隔离增强)

**交付物**:

- ✅ 流式处理实现
- ✅ 成本追踪实现
- ⚠️ 安全机制实现 (基础权限系统)
- ❌ 性能优化
- ❌ 文档完善

### 7.5 第五阶段：应用和集成 🔄 **进行中**

**目标**: 实现 CLI 和 Server 应用，集成完整功能

**任务**:

- [x] 实现 CLI 应用 → `apps/turbot-cli/`
  - [x] 命令解析 → `commands.cpp`
  - [ ] 交互式界面 (待完善)
  - [x] 配置管理 → 集成 `ConfigManager`
- [x] 实现 Server 应用 → `apps/turbot-server/`
  - [x] HTTP API → `server.cpp`
  - [ ] WebSocket 支持
  - [ ] 认证授权
- [ ] 实现 IDE 集成
  - [ ] LSP 协议
  - [ ] VSCode 扩展
- [ ] 完善文档
- [ ] 示例和教程

**交付物**:

- ✅ CLI 应用基础实现
- ✅ Server 应用基础实现
- ❌ IDE 集成支持
- ❌ 完整文档
- ❌ 示例项目

### 7.6 进度统计

| 阶段                    | 任务数 | 完成   | 进度    | 状态      |
| ----------------------- | ------ | ------ | ------- | --------- |
| 第一阶段: 基础架构      | 8      | 8      | 100%    | ✅ 完成   |
| 第二阶段: 核心组件      | 14     | 14     | 100%    | ✅ 完成   |
| 第三阶段: 多 Agent 协作 | 10     | 7      | 70%     | 🔄 进行中 |
| 第四阶段: 高级特性      | 7      | 2      | 29%     | 🔄 进行中 |
| 第五阶段: 应用和集成    | 11     | 4      | 36%     | 🔄 进行中 |
| **总计**                | **50** | **35** | **70%** | 🔄 进行中 |

### 7.7 代码统计

```
库文件: 91 个源文件 (libs/)
应用文件: 5 个源文件 (apps/)
单元测试: 20 个测试文件 (tests/unit/)
测试覆盖: 464 test cases, 2124 assertions
```

### 7.8 下一步优先事项

1. **快照和回滚** - 文件系统快照、补丁生成
2. **缓存机制** - Prompt 缓存、模型信息缓存
3. **交互式界面** - CLI 增强
4. **WebSocket 支持** - Server 实时通信
5. **IDE 集成** - LSP 协议、VSCode 扩展

## 8. 性能优化策略

### 8.1 内存管理

- 使用智能指针管理资源
- 实现对象池减少分配开销
- 使用移动语义避免拷贝
- 限制消息历史大小

### 8.2 并发处理

- 使用协程实现异步 IO
- 线程池处理 CPU 密集型任务
- 读写锁保护共享数据
- 无锁数据结构（可选）

### 8.3 缓存策略

- Prompt 缓存
- 文件内容缓存
- 模型信息缓存
- 工具注册表缓存

### 8.4 数据库优化

- 索引优化
- 查询优化
- 连接池
- 批量操作

## 9. 安全机制

### 9.1 权限系统

- 三层权限控制
- 通配符匹配
- 最小权限原则
- 审计日志

### 9.2 认证授权

- API Key 认证
- OAuth 2.0 支持
- Token 刷新
- 请求签名

### 9.3 输入验证

- JSON Schema 验证
- 参数类型检查
- SQL 注入防护
- XSS 防护

### 9.4 沙箱隔离

- 文件访问限制
- 命令执行限制
- 网络访问限制
- 资源使用限制

## 10. 监控和可观测性

### 10.1 日志系统

- 结构化日志
- 日志级别
- 日志轮转
- 分布式追踪

### 10.2 指标收集

- 请求延迟
- 错误率
- Token 使用
- 成本追踪

### 10.3 健康检查

- 数据库连接
- AI 提供商连接
- 磁盘空间
- 内存使用

## 11. 测试策略

### 11.1 单元测试

- 核心组件测试
- 工具函数测试
- 边界条件测试
- 异常处理测试

### 11.2 集成测试

- 组件间交互测试
- 数据库操作测试
- API 测试
- 端到端测试

### 11.3 性能测试

- 压力测试
- 内存泄漏检测
- 响应时间测试
- 并发测试

### 11.4 安全测试

- 权限绕过测试
- 注入攻击测试
- DoS 攻击测试
- 数据泄露测试

## 12. 部署方案

### 12.1 本地部署

```bash
# 安装依赖
conan install . --output-folder=build/conan --build=missing

# 构建项目
cmake --preset=release-conan
cmake --build build/release

# 运行CLI
./build/release/bin/turbot-cli run

# 运行服务器
./build/release/bin/turbot-server --port 4096
```

### 12.2 Docker 部署

```dockerfile
FROM ubuntu:22.04

RUN apt-get update && apt-get install -y \
    cmake \
    clang-18 \
    libssl-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . .

RUN conan install . --output-folder=build/conan --build=missing
RUN cmake --preset=release-conan
RUN cmake --build build/release

CMD ["./build/release/bin/turbot-server", "--port", "4096"]
```

### 12.3 云部署

- AWS EC2/ECS
- Google Cloud Run
- Azure Container Instances
- Kubernetes

## 13. 风险和挑战

### 13.1 技术风险

- **C++23 兼容性**: 部分编译器可能不完全支持 C++23
- **协程性能**: 协程实现的性能需要充分测试
- **异步编程复杂度**: 协程和异步代码的调试难度较高

### 13.2 实现挑战

- **内存安全**: C++的内存管理需要格外小心
- **线程安全**: 多线程环境下的数据一致性
- **错误处理**: 异步环境下的错误传播

### 13.3 缓解措施

- 使用静态分析工具（clang-tidy）
- 完善的单元测试和集成测试
- 代码审查和同行评审
- 性能测试和压力测试

## 14. 总结

本技术方案基于对 OpenCode 项目的深入分析，为 Turbot-AI 项目规划了一个完整的 C++23 多 Agent 协作架构实现方案。方案涵盖了从基础架构到高级特性的各个方面，包括：

- 清晰的分层架构设计
- 完整的核心组件实现
- 强大的多 Agent 协作机制
- 健壮的数据持久层
- 全面的安全机制
- 详细的实现路线图

通过遵循本方案，Turbot-AI 项目将能够构建一个高性能、安全、可扩展的 AI 编码助手框架，为开发者提供强大的 AI 辅助编程能力。

## 15. 参考文档

- OpenCode 项目架构分析文档
- C++23 标准文档
- Asio 库文档
- nlohmann/json 库文档
- spdlog 库文档
- SQLite 文档
