# 3.1 MCP 传输层与 JSON-RPC 核心

> **所属版本**: v3.0 - MCP 子计划 1/4
> **创建时间**: 2026-03-14
> **依赖**: 无（首个独立计划）
> **后续计划**: [3.2-mcp-client-plan.md](./3.2-mcp-client-plan.md)、[3.3-mcp-oauth-plan.md](./3.3-mcp-oauth-plan.md)
> **估时**: 2 天
> **测试类型**: 单元测试（覆盖率 ≥ 80%）

---

## 目标

实现三种 MCP 传输层（stdio/SSE/StreamableHTTP），支持 JSON-RPC 2.0 消息收发，为后续 `MCPClient` 和 `MCPManager` 提供可测试的传输抽象。

---

## 文件结构

```
libs/core/include/turbot/core/mcp/
├── mcp.hpp              # 公共类型：MCPStatus, MCPTool, MCPResource, MCPPrompt, JsonRpc*
├── transport.hpp        # ITransport 抽象接口
├── stdio_transport.hpp  # StdioTransport：fork 子进程，pipe 通信
├── sse_transport.hpp    # SSETransport：基于 HttpClient SSE 流式
└── http_transport.hpp   # HttpTransport：Streamable HTTP POST（含 SSE fallback）

libs/core/src/mcp/
├── mcp.cpp
├── transport.cpp
├── stdio_transport.cpp
├── sse_transport.cpp
└── http_transport.cpp

tests/unit/mcp/
├── test_mcp_types.cpp
├── test_stdio_transport.cpp
├── test_sse_transport.cpp
└── test_http_transport.cpp
```

---

## 实施步骤

### 步骤 1：定义公共类型（mcp.hpp）

**实施内容**：

- `MCPStatus` 枚举（Connected/Disabled/Failed/NeedsAuth/NeedsClientRegistration）
- `MCPTool`（name, description, input_schema）及 `to_json()`/`from_json()`
- `MCPResource`（name, uri, description, mime_type, client）
- `MCPPrompt`（name, description, arguments）
- `MCPClientConfig`（type, command, environment, url, oauth, headers, enabled, timeout_ms=30000）
- `JsonRpcRequest`/`JsonRpcResponse` 基础结构

**关键约定**：

- `DEFAULT_TIMEOUT = 30000`（对齐 OpenCode DEFAULT_TIMEOUT = 30_000）
- `MCPClientConfig::timeout_ms` 默认 30000
- MCP 工具命名格式：`{sanitized_client}_{sanitized_tool}`，sanitize = `replace(/[^a-zA-Z0-9_-]/g, "_")`

**验收**：类型定义头文件编译通过，无循环依赖

---

### 步骤 2：定义 ITransport 抽象接口（transport.hpp）

```cpp
class ITransport {
public:
    virtual ~ITransport() = default;
    virtual std::future<void> connect() = 0;
    virtual std::future<JsonRpcResponse> send_request(
        const std::string& method,
        const nlohmann::json& params = nlohmann::json::object()
    ) = 0;
    virtual void send_notification(
        const std::string& method,
        const nlohmann::json& params = nlohmann::json::object()
    ) = 0;
    virtual void on_notification(
        const std::string& method,
        std::function<void(const nlohmann::json&)> handler
    ) = 0;
    virtual std::future<void> close() = 0;
    virtual int pid() const noexcept { return -1; }  // 仅 StdioTransport 返回有效 PID
};
```

**验收**：抽象接口编译通过，含纯虚析构

---

### 步骤 3：实现 StdioTransport（stdio_transport.cpp）

**设计要点**：

- `fork + execvp` 启动子进程，持有 stdin/stdout/stderr pipe fd
- 独立 io 读线程：持续从 stdout 读取 newline-delimited JSON，按 `id` 派发到 pending `promise`
- 通知消息（无 `id` 字段）：调用对应 `on_notification` 处理器
- stderr 异步日志（对齐 OpenCode `transport.stderr?.on("data", ...)`）
- 进程退出时清理所有 pending futures（以 error response 完成）
- `pid()` 返回子进程 PID（供 `MCPManager` 清理孙进程，对齐 OpenCode `descendants(pid)` + SIGTERM 逻辑）

**消息帧格式**（Newline-Delimited JSON）：

```
{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}\n
```

**实施内容**：

1. `connect()`：fork + execvp，启动读线程
2. `send_request()`：原子自增 id，写入 stdout pipe，注册 promise，返回 future
3. `send_notification()`：写入 stdout pipe，无 id
4. 读线程：readline → JSON 解析 → 按 id 分发或调用通知 handler
5. `close()`：关闭 stdin fd，发 SIGTERM，等待进程退出

**验收**：

- Mock 子进程（echo server）测试请求/响应往返
- 不合法 JSON 不 crash，记录错误日志并继续

---

### 步骤 4：实现 SSETransport（sse_transport.cpp）

**设计要点**：

- 建立 SSE 长连接（`GET {url}` with `Accept: text/event-stream`）
- 通过 `HttpClient::request_stream()` 接收 SSE 推送消息
- 请求通过独立 POST `{url}` 发出
- SSE `data:` 字段解析为 JSON-RPC 消息，分发逻辑与 StdioTransport 一致

**实施内容**：

1. `connect()`：建立 SSE 连接，启动读循环
2. `send_request()`：POST `{url}` + JSON body，注册 promise 等待 SSE 推送
3. SSE 读循环：解析 `data: {...}` 行，触发分发

**验收**：

- Mock SSE 服务器（HTTP test double）测试完整请求-响应周期
- SSE 连接断开时自动触发 close 清理

---

### 步骤 5：实现 HttpTransport（http_transport.cpp，含 SSE fallback）

**设计要点**：

- 先尝试 `StreamableHTTP`：POST `{url}`，Content-Type: application/json，期望响应 `application/json`（同步响应）
- 若响应为 `text/event-stream` 或连接失败，降级到 SSETransport（对齐 OpenCode `StreamableHTTPClientTransport` → `SSEClientTransport` fallback）
- 降级策略：首次 connect 时检测，一旦确定协议类型后固定

**实施内容**：

1. `connect()`：尝试 StreamableHTTP，失败则切换到 SSE 模式
2. `send_request()`：根据已确定的协议模式路由

**验收**：

- 对支持 Streamable HTTP 的 Mock 服务器，优先使用 HTTP 模式
- 对仅 SSE 服务器，自动降级

---

### 步骤 6：单元测试（tests/unit/mcp/）

**测试清单**：

| 测试文件                   | 关键测试 case                                 |
| -------------------------- | --------------------------------------------- |
| `test_mcp_types.cpp`       | MCPTool/Resource/Prompt 序列化/反序列化       |
| `test_stdio_transport.cpp` | 启动 echo 子进程、请求/响应往返、进程退出清理 |
| `test_sse_transport.cpp`   | Mock SSE 服务器、事件接收和通知分发           |
| `test_http_transport.cpp`  | StreamableHTTP 优先、SSE fallback 逻辑        |

---

## 验收标准

1. `StdioTransport` 能启动 `npx -y @playwright/mcp@latest`（如已安装）并收到 JSON-RPC initialize 响应
2. `HttpTransport` 先尝试 StreamableHTTP，降级到 SSE（对齐 OpenCode fallback 逻辑）
3. 所有单元测试通过，覆盖率 ≥ 80%
4. 消息帧解析：不合法 JSON 不 crash，记录错误继续

---

## 代码审查记录

| 审查轮次 | SPEARM 综合分 | 判定 | C-/D 触发 | 发现问题 | 修复情况 |
| -------- | ------------- | ---- | --------- | -------- | -------- |
| -        | -             | -    | -         | -        | -        |

**当前状态**：⏸️ 待执行
