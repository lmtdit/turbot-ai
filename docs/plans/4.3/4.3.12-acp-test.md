# v4.3.12 ACP 协议测试

> **依赖**: 4.3.3 (会话核心测试)
> **预估工时**: 1 天
> **状态**: 待执行

---

## 目标

验证 Turbot ACP (Agent Client Protocol) 协议实现。

---

## C++ 适配说明

| OpenCode (TypeScript) | Turbot (C++23)                       |
| --------------------- | ------------------------------------ |
| `EventEmitter` 事件   | `std::function` 回调 + `std::signal` |
| `Event.subscribe()`   | `PubSub<T>` 模板类                   |
| 异步消息处理          | `std::future` + 消息队列             |
| IDE 通信              | Unix Socket 或 Named Pipe            |

---

## 参考文件

| OpenCode 测试文件                | 用途           |
| -------------------------------- | -------------- |
| `acp/agent-interface.test.ts`    | Agent 接口测试 |
| `acp/event-subscription.test.ts` | 事件订阅测试   |

---

## 任务清单

### 任务 1: ACP Agent 接口测试

**参考**: `acp/agent-interface.test.ts`

**测试用例**:

| 用例名              | 测试内容 | 预期结果         |
| ------------------- | -------- | ---------------- |
| ACP_CreateSession   | 创建会话 | 会话正确创建     |
| ACP_SendMessage     | 发送消息 | 消息正确发送     |
| ACP_ReceiveResponse | 接收响应 | 响应正确接收     |
| ACP_StreamEvents    | 流式事件 | 事件正确流式传输 |
| ACP_CancelRequest   | 取消请求 | 请求正确取消     |
| ACP_CloseSession    | 关闭会话 | 会话正确关闭     |

**验证标准**:

- [ ] ACP Agent 接口测试通过

---

### 任务 2: ACP 事件订阅测试

**参考**: `acp/event-subscription.test.ts` (20.8KB)

**测试用例**:

| 用例名                  | 测试内容 | 预期结果         |
| ----------------------- | -------- | ---------------- |
| ACP_Subscribe           | 订阅事件 | 事件订阅成功     |
| ACP_Unsubscribe         | 取消订阅 | 取消订阅成功     |
| ACP_EventDelivery       | 事件投递 | 事件正确投递     |
| ACP_MultipleSubscribers | 多订阅者 | 多订阅者正确处理 |
| ACP_EventFilter         | 事件过滤 | 过滤条件正确     |
| ACP_EventBuffer         | 事件缓冲 | 缓冲机制正确     |

**输出文件**:

- `tests/v2/acp/acp_test.cpp`

**验证标准**:

- [ ] ACP 事件订阅测试通过

---

## 验证命令

```bash
cmake --build build --target turbot-v2-tests
./build/tests/v2/turbot-v2-tests "[ACP]"
```

---

## 完成标准

| 指标       | 目标值     |
| ---------- | ---------- |
| 测试用例数 | 12+ 个通过 |
| 测试覆盖率 | > 90%      |
