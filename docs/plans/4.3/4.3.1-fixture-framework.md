# v4.3.1 测试框架搭建

> **依赖**: 无
> **预估工时**: 1 天
> **状态**: 待执行

---

## 目标

搭建 Turbot V2 测试系统的基础框架，为后续测试提供测试夹具和 CMake 配置。

---

## C++ 适配说明

| OpenCode (TypeScript)                 | Turbot (C++23)                     |
| ------------------------------------- | ---------------------------------- |
| `tmpdir({ git: true })`               | `TmpDir(true)` - RAII 自动清理     |
| `await using tmp = ...`               | 栈对象，析构时自动清理             |
| `Instance.provide({ directory, fn })` | `TestInstance::provide()` + 宏封装 |
| `Effect.gen` 异步生成器               | 同步实现，测试中不需要异步         |

---

## 参考文件

| OpenCode 测试文件                            | 用途         |
| -------------------------------------------- | ------------ |
| `opencode/packages/opencode/test/fixture.ts` | 测试夹具实现 |
| `opencode/packages/opencode/test/fixture/`   | 夹具目录结构 |

---

## 任务清单

### 任务 1: 创建目录结构

**目标**: 创建 `tests/v2/` 目录及子目录

**执行步骤**:

1. 创建 `tests/v2/` 根目录
2. 创建子目录: `fixture/`, `permission/`, `session/`, `tool/`, `file/`, `agent/`, `mcp/`, `skill/`

**验证标准**:

- [ ] 目录结构创建完成
- [ ] 所有子目录存在

---

### 任务 2: 实现测试夹具 - TmpDir

**目标**: 实现临时目录管理类，类似 OpenCode 的 `tmpdir()`

**参考**: `opencode/packages/opencode/test/fixture.ts` 中的 `tmpdir` 函数

**功能要求**:

- 创建临时目录
- 支持 `git_init` 参数初始化 git 仓库
- RAII 自动清理

**输出文件**:

- `tests/v2/fixture/test_fixture.hpp` - 声明
- `tests/v2/fixture/test_fixture.cpp` - 实现

**验证标准**:

- [ ] TmpDir 类编译通过
- [ ] 临时目录创建成功
- [ ] 析构时目录自动清理

---

### 任务 3: 实现测试夹具 - TestContext

**目标**: 实现测试上下文结构，模拟 OpenCode 的 `Tool.Context`

**参考**: OpenCode 中工具测试使用的上下文对象

**功能要求**:

- 包含 session_id, message_id, call_id, agent 字段
- 实现 `ask()` 方法收集权限请求
- 支持消息列表

**输出文件**:

- 追加到 `tests/v2/fixture/test_fixture.hpp/cpp`

**验证标准**:

- [ ] TestContext 结构编译通过
- [ ] ask() 方法正确收集权限请求

---

### 任务 4: 实现测试夹具 - TestInstance

**目标**: 实现 Instance 模拟，类似 OpenCode 的 `Instance.provide()`

**参考**: `opencode/packages/opencode/test/fixture.ts` 中的 `Instance` 对象

**功能要求**:

- 单例模式
- `provide()` 方法设置当前目录
- `contains_path()` 方法检测路径是否在项目内

**输出文件**:

- 追加到 `tests/v2/fixture/test_fixture.hpp/cpp`

**验证标准**:

- [ ] TestInstance 类编译通过
- [ ] contains_path() 正确检测路径边界

---

### 任务 5: 创建测试宏

**目标**: 创建测试辅助宏，简化测试代码

**参考**: OpenCode 测试中常用的模式

**功能要求**:

- `TURBOT_TEST_PROVIDE(dir, code)` - 设置测试目录
- `TURBOT_TEST_TMPDIR(name, git_init)` - 创建临时目录

**输出文件**:

- `tests/v2/fixture/test_macros.hpp`

**验证标准**:

- [ ] 宏定义编译通过
- [ ] 宏展开正确

---

### 任务 6: 创建 CMakeLists.txt

**目标**: 配置 CMake 构建系统

**参考**: `turbot-ai/tests/CMakeLists.txt` 现有结构

**功能要求**:

- 创建 `turbot-v2-tests` 可执行目标
- 链接 Catch2、nlohmann_json
- 链接 turbot::core, turbot::utils, turbot::storage
- 配置 C++23 标准（备选 C++20）
- 注册 catch_discover_tests

**输出文件**:

- `tests/v2/CMakeLists.txt`
- 更新 `tests/CMakeLists.txt` 添加 `add_subdirectory(v2)`

**验证标准**:

- [ ] CMake 配置正确
- [ ] `cmake --build build --target turbot-v2-tests` 成功
- [ ] 测试可执行文件生成

---

## 验证命令

```bash
# 配置 CMake
cmake -B build -S .

# 编译测试
cmake --build build --target turbot-v2-tests

# 运行测试（应无测试用例或只有基础测试）
./build/tests/v2/turbot-v2-tests
```

---

## 完成标准

| 指标       | 目标值                    |
| ---------- | ------------------------- |
| 编译通过   | 0 errors                  |
| 目录结构   | 8 个子目录                |
| 夹具文件   | 3 个文件 (hpp/cpp/macros) |
| CMake 配置 | 正确链接依赖              |

---

## 后续依赖

完成后可执行:

- [4.3.2 权限系统与会话分页测试](./4.3.2-permission-session-test.md)
