# v4.3.8 LLM Provider 测试

> **依赖**: 4.3.3 (会话核心测试)
> **预估工时**: 1 天
> **状态**: 待执行

---

## 目标

验证 Turbot LLM Provider 系统，包括多 Provider 支持、认证、转换等。

---

## C++ 适配说明

| OpenCode (TypeScript)    | Turbot (C++23)                           |
| ------------------------ | ---------------------------------------- |
| `mock.http()` HTTP Mock  | `MockHttpClient` 接口注入                |
| `fetch()` 网络请求       | `IHttpClient` 接口抽象                   |
| Provider 配置 JSON       | `ProviderConfig` 结构体 + `std::variant` |
| 动态加载 Provider        | 编译期注册 + 工厂模式                    |
| `transform.test.ts` 75KB | 按能力拆分为多个测试文件                 |

---

## 参考文件

| OpenCode 测试文件                 | 用途            |
| --------------------------------- | --------------- |
| `provider/provider.test.ts`       | Provider 测试   |
| `provider/transform.test.ts`      | 转换测试        |
| `provider/amazon-bedrock.test.ts` | Bedrock 测试    |
| `provider/gitlab-duo.test.ts`     | GitLab Duo 测试 |
| `provider/auth.test.ts`           | 认证测试        |

---

## 任务清单

### 任务 1: Provider 核心测试

**参考**: `provider/provider.test.ts` (64.6KB)

**测试用例**:

| 用例名               | 测试内容            | 预期结果              |
| -------------------- | ------------------- | --------------------- |
| Provider_OpenAI      | OpenAI Provider     | 正确调用 OpenAI       |
| Provider_Anthropic   | Anthropic Provider  | 正确调用 Anthropic    |
| Provider_Gemini      | Gemini Provider     | 正确调用 Gemini       |
| Provider_OpenRouter  | OpenRouter Provider | 正确调用 OpenRouter   |
| Provider_Fallback    | 降级处理            | Provider 不可用时降级 |
| Provider_LoadBalance | 负载均衡            | 多 Provider 负载均衡  |

**验证标准**:

- [ ] Provider 核心测试通过

---

### 任务 2: 消息转换测试

**参考**: `provider/transform.test.ts` (75.7KB，最大文件)

**测试用例**:

| 用例名               | 测试内容           | 预期结果          |
| -------------------- | ------------------ | ----------------- |
| Transform_OpenAI     | OpenAI 格式转换    | 消息正确转换      |
| Transform_Anthropic  | Anthropic 格式转换 | 消息正确转换      |
| Transform_Gemini     | Gemini 格式转换    | 消息正确转换      |
| Transform_ToolCall   | 工具调用转换       | 工具调用正确转换  |
| Transform_MultiModal | 多模态转换         | 图像/文件正确转换 |

**验证标准**:

- [ ] 消息转换测试通过

---

### 任务 3: 特殊 Provider 测试

**参考**: `provider/amazon-bedrock.test.ts`, `provider/gitlab-duo.test.ts`

**测试用例**:

| 用例名         | 测试内容     | 预期结果         |
| -------------- | ------------ | ---------------- |
| Bedrock_Auth   | Bedrock 认证 | AWS 认证正确     |
| Bedrock_Invoke | Bedrock 调用 | 正确调用 Bedrock |
| GitLabDuo_Auth | GitLab 认证  | GitLab 认证正确  |
| GitLabDuo_Chat | GitLab Chat  | 聊天功能正确     |

**输出文件**:

- `tests/v2/provider/provider_test.cpp`

**验证标准**:

- [ ] 特殊 Provider 测试通过

---

## 验证命令

```bash
cmake --build build --target turbot-v2-tests
./build/tests/v2/turbot-v2-tests "[Provider]"
```

---

## 完成标准

| 指标       | 目标值     |
| ---------- | ---------- |
| 测试用例数 | 15+ 个通过 |
| 测试覆盖率 | > 90%      |
