# v4.3.7 文件系统测试

> **依赖**: 4.3.1 (测试框架)
> **预估工时**: 1 天
> **状态**: 待执行

---

## 目标

验证 Turbot 文件系统功能，包括文件监控、忽略规则、索引、搜索等。

---

## C++ 适配说明

| OpenCode (TypeScript) | Turbot (C++23)                           |
| --------------------- | ---------------------------------------- |
| `chokidar` 文件监控   | `std::filesystem` + `inotify`/`FSEvents` |
| `.gitignore` 解析     | 自实现 `gitignore` 匹配器                |
| 文件索引              | `std::unordered_map` + 增量更新          |
| `ripgrep` 集成        | 调用 `rg` 命令行或自实现                 |

---

## 参考文件

| OpenCode 测试文件        | 用途         |
| ------------------------ | ------------ |
| `file/index.test.ts`     | 文件索引测试 |
| `file/fsmonitor.test.ts` | 文件监控测试 |
| `file/ignore.test.ts`    | 忽略规则测试 |
| `file/ripgrep.test.ts`   | Ripgrep 测试 |
| `file/time.test.ts`      | 时间处理测试 |

---

## 任务清单

### 任务 1: 文件索引测试

**参考**: `file/index.test.ts` (12.7KB)

**测试用例**:

| 用例名                  | 测试内容 | 预期结果             |
| ----------------------- | -------- | -------------------- |
| Index_BuildIndex        | 构建索引 | 正确索引文件         |
| Index_IncrementalUpdate | 增量更新 | 文件变更正确更新索引 |
| Index_Query             | 查询索引 | 查询结果正确         |
| Index_RemoveEntry       | 删除条目 | 删除正确处理         |

**验证标准**:

- [ ] 文件索引测试通过

---

### 任务 2: 文件监控测试

**参考**: `file/fsmonitor.test.ts`

**测试用例**:

| 用例名                 | 测试内容 | 预期结果         |
| ---------------------- | -------- | ---------------- |
| FSMonitor_StartWatch   | 开始监控 | 监控正确启动     |
| FSMonitor_DetectChange | 检测变更 | 文件变更检测正确 |
| FSMonitor_DetectCreate | 检测创建 | 新文件检测正确   |
| FSMonitor_DetectDelete | 检测删除 | 删除检测正确     |
| FSMonitor_StopWatch    | 停止监控 | 监控正确停止     |

**验证标准**:

- [ ] 文件监控测试通过

---

### 任务 3: 忽略规则测试

**参考**: `file/ignore.test.ts`

**测试用例**:

| 用例名              | 测试内容           | 预期结果              |
| ------------------- | ------------------ | --------------------- |
| Ignore_Gitignore    | .gitignore 规则    | 正确解析 gitignore    |
| Ignore_Turbotignore | .turbotignore 规则 | 正确解析 turbotignore |
| Ignore_PatternMatch | 模式匹配           | 通配符匹配正确        |

**验证标准**:

- [ ] 忽略规则测试通过

---

### 任务 4: Ripgrep 测试

**参考**: `file/ripgrep.test.ts`

**测试用例**:

| 用例名         | 测试内容 | 预期结果     |
| -------------- | -------- | ------------ |
| Ripgrep_Search | 搜索功能 | 搜索结果正确 |
| Ripgrep_Regex  | 正则搜索 | 正则匹配正确 |

**输出文件**:

- `tests/v2/file/filesystem_test.cpp`

**验证标准**:

- [ ] Ripgrep 测试通过

---

## 验证命令

```bash
cmake --build build --target turbot-v2-tests
./build/tests/v2/turbot-v2-tests "[File]"
```

---

## 完成标准

| 指标       | 目标值     |
| ---------- | ---------- |
| 测试用例数 | 15+ 个通过 |
| 测试覆盖率 | > 90%      |
