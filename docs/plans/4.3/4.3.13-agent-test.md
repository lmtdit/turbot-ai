# v4.3.13 Agent 系统测试

> **依赖**: 4.3.2 (权限系统测试)
> **预估工时**: 1 天
> **状态**: 待执行

---

## 目标

验证 Turbot Agent 系统，包括默认 Agent、自定义 Agent、权限控制等。

---

## C++ 适配说明

| OpenCode (TypeScript) | Turbot (C++23)             |
| --------------------- | -------------------------- |
| Agent 配置对象        | `AgentConfig` 结构体       |
| 动态 Agent 加载       | 配置文件解析 + 工厂模式    |
| Agent 权限规则        | `PermissionRule` 组合      |
| Agent 状态机          | `std::variant<AgentState>` |

---

## 参考文件

| OpenCode 测试文件     | 用途           |
| --------------------- | -------------- |
| `agent/agent.test.ts` | Agent 系统测试 |

---

## 任务清单

### 任务 1: 默认 Agent 测试

**参考**: `agent/agent.test.ts`

**测试用例**:

| 用例名                  | 测试内容           | 预期结果                  |
| ----------------------- | ------------------ | ------------------------- |
| Agent_ListDefaults      | 列出默认 Agent     | build, plan, explore 存在 |
| Agent_BuildProperties   | build Agent 属性   | mode=Primary, native=true |
| Agent_PlanProperties    | plan Agent 属性    | mode=Primary, 编辑受限    |
| Agent_ExploreProperties | explore Agent 属性 | mode=Subagent, 只读       |
| Agent_DefaultAgent      | 默认 Agent         | 返回 build                |

**验证标准**:

- [ ] 默认 Agent 测试通过

---

### 任务 2: Agent 权限控制测试

**测试用例**:

| 用例名                    | 测试内容             | 预期结果         |
| ------------------------- | -------------------- | ---------------- |
| Agent_PlanDeniesEdits     | plan 禁止编辑        | 通配符 deny      |
| Agent_PlanAllowsPlans     | plan 允许 plans 目录 | plans 目录 allow |
| Agent_ExploreDeniesEdits  | explore 禁止编辑     | edit/\* deny     |
| Agent_ExploreDeniesWrites | explore 禁止写入     | write/\* deny    |
| Agent_BuildAllowsAll      | build 允许所有       | 默认 allow       |

**验证标准**:

- [ ] Agent 权限控制测试通过

---

### 任务 3: 自定义 Agent 测试

**测试用例**:

| 用例名                 | 测试内容   | 预期结果              |
| ---------------------- | ---------- | --------------------- |
| Agent_CustomFromConfig | 从配置创建 | 自定义 Agent 正确加载 |
| Agent_Disable          | 禁用 Agent | 禁用后不在列表        |
| Agent_Override         | 覆盖默认   | 覆盖默认 Agent        |
| Agent_InvalidConfig    | 无效配置   | 配置错误处理          |

**输出文件**:

- `tests/v2/agent/agent_test.cpp`

**验证标准**:

- [ ] 自定义 Agent 测试通过

---

## 验证命令

```bash
cmake --build build --target turbot-v2-tests
./build/tests/v2/turbot-v2-tests "[Agent]"
```

---

## 完成标准

| 指标       | 目标值     |
| ---------- | ---------- |
| 测试用例数 | 13+ 个通过 |
| 测试覆盖率 | > 90%      |
