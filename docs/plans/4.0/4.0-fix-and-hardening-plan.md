# 4.0 修复与加固计划

> **创建时间**：2026-03-14
> **状态**：📋 待启动
> **当前进度**：0%（0/13 问题解决）
> **背景**：基于 [2026-03-14 整体评估报告](../../reviews/2026-03-14-turbot-overall-review.md)，修复 v2.x（综合分 80/100）+ v3.0（综合分 70/100，上限锁定）全部 P0/P1/P2 问题

---

## 总览表

| ID   | 问题                                   | 优先级 | 影响版本    | 工时   | 状态      |
| ---- | -------------------------------------- | ------ | ----------- | ------ | --------- |
| P0-1 | Session::get/list/delete 占位          | P0     | v2.x + v3.0 | 3 天   | 📋 待启动 |
| P0-2 | 权限系统旁路（ask_permission 未注入）  | P0     | v2.x + v3.0 | 2 天   | 📋 待启动 |
| P0-3 | v2.9 修复 7 个失败测试（CMakeLists）   | P0     | v2.x        | 0.5 天 | 📋 待启动 |
| P0-4 | MCP 单元测试目录为空                   | P0     | v3.0        | 2 天   | 📋 待启动 |
| P1-1 | BuildAgent/ExploreAgent/PlanAgent 占位 | P1     | v2.x + v3.0 | 3 天   | 📋 待启动 |
| P1-2 | ACP cancel() 中断信号为 stub           | P1     | v3.0        | 1 天   | 📋 待启动 |
| P1-3 | set_on_tool_result diff 内容为空       | P1     | v3.0        | 1 天   | 📋 待启动 |
| P1-4 | generate_agent() LLM 接入              | P1     | v2.x        | 1.5 天 | 📋 待启动 |
| P1-5 | v2.8 计划文档状态未同步                | P1     | 文档        | 0.5 天 | 📋 待启动 |
| P1-6 | Provider 初始化与端到端 LLM 验证       | P1     | v2.x + v3.0 | 1.5 天 | 📋 待启动 |
| P2-1 | multiedit/webfetch/codesearch 工具缺失 | P2     | v2.x        | 3 天   | 📋 待启动 |
| P2-2 | LSP 语言服务器 3→8 种扩展              | P2     | v3.0        | 2 天   | 📋 待启动 |
| P2-3 | SSE fallback 降级逻辑集成测试          | P2     | v3.0        | 1 天   | 📋 待启动 |

**总工时估算：约 22 天**

---

## 依赖关系与执行顺序

```
P0-3（修复 CMakeLists）  ← 独立，最先执行，解锁 v2.9 后续测试
P0-4（MCP 单元测试）     ← 独立，可与 P0-3 并行
P0-1（Session DB）       ← P0-3/P0-4 完成后执行，是 P0-2/P1-2 的前置
P0-2（权限注入）         ← 依赖 P0-1（Session 层稳定后再建立权限链路）
P1-5（文档同步）         ← 独立，可随时执行
P1-2（cancel 信号）      ← 依赖 P0-1
P1-3（diff 内容）        ← 独立，可在 P0-1 之前执行
P1-4（generate_agent）   ← 独立
P1-6（Provider 初始化）  ← 独立，是 P1-1 的前置依赖
P1-1（内置 Agent）       ← 依赖 P0-1 + P0-2 + P1-6
P2-x                     ← P0/P1 全部完成后执行
```

**推荐执行顺序**：P0-3 → P0-4 → P0-1 → P0-2 → P1-5 → P1-3 → P1-2 → P1-4 → P1-6 → P1-1 → P2-1 → P2-2 → P2-3

---

## P0-1：Session 持久化 [CRITICAL]

> **状态**：📋 待启动
> **依赖**：无
> **工时**：3 天
> **影响版本**：v2.x + v3.0

### 问题描述

`libs/core/src/session/session.cpp` line 231-250 中三个方法均为占位：

```cpp
// This is a placeholder - in a real implementation, this would query the database
(void)id;
return std::nullopt;  // Session::get()
return {};            // Session::list()
return false;         // Session::delete()
```

同一文件 line 276-285 的 `Session::messages()` 也返回空：

```cpp
return {};  // placeholder
```

**影响链路**：

- v2.x：多 Session 管理不可用，会话历史无法从 DB 恢复
- v3.0：`ACP.load_session → Session::get() → nullopt → 抛异常`；`ACP.list_sessions → Session::list() → 永远空列表`；`ACP.fork_session` 永远失败

### 修复方向

`libs/storage/` SQLite 层已存在，接入路径：

1. 在 `session.cpp` 中注入 `IDatabase` 依赖（构造函数或 Setter）
2. 实现 `Session::get(id)` → `SELECT * FROM sessions WHERE id = ?`
3. 实现 `Session::list()` → `SELECT * FROM sessions ORDER BY created_at DESC`
4. 实现 `Session::delete(id)` → `DELETE FROM sessions WHERE id = ?`
5. 实现 `Session::messages(session_id)` → `SELECT * FROM messages WHERE session_id = ? ORDER BY seq`
6. 在 `SessionLoop` 启动时持久化新 Session，在每轮 step 后持久化消息

### 验收标准

- [ ] 编译通过
- [ ] `ACP.new_session` 后数据库有记录
- [ ] `ACP.list_sessions` 返回非空列表
- [ ] `ACP.load_session` 成功回放历史消息
- [ ] `ACP.fork_session` 可复制已存在的 Session
- [ ] 相关单元测试通过

---

## P0-2：权限系统注入 [CRITICAL]

> **状态**：📋 待启动
> **依赖**：P0-1（Session 层稳定）
> **工时**：2 天
> **影响版本**：v2.x + v3.0

### 问题描述

`libs/core/src/session/session_loop.cpp` line 700 构建 `ToolContext` 时，`ask_permission` 回调从未设置：

```cpp
tool::ToolContext ctx;
ctx.session_id        = session_.id();
ctx.message_id        = fmt::format("msg_{}", ...);
ctx.agent             = local_agent ? local_agent->name() : "unknown";
ctx.call_id           = call_id;
ctx.abort_flag        = abort_flag_;
ctx.working_directory = session_.info().directory;
// ask_permission 从未设置 ← 所有工具跳过权限确认
```

各工具（read/write/edit/bash/glob/grep/list）均有如下守卫：

```cpp
if (!ctx.ask_permission) {
    return PermissionResult::allow();  // 直接放行，permission.cpp 规则引擎从未触发
}
```

**OpenCode 对比**：完整闭环为 `permission.asked` EventBus 事件 → ACP `requestPermission()` → 用户确认 → 权限回复

### 修复方向

1. 在 `SessionLoop` 中增加权限回调注入点（构造时或 `run()` 前）
2. 实现 `ask_permission` lambda：
   - 通过 EventBus 发布 `permission.asked` 事件，携带工具名、参数、session_id
   - 等待 `permission.granted` / `permission.denied` 事件回复（使用 `std::promise` / `condition_variable`）
3. ACP 侧（`acp/agent.cpp`）监听 `permission.asked`，向客户端发送 `requestPermission` 通知
4. ACP 侧处理客户端回复，发布 `permission.granted` 或 `permission.denied`

### 验收标准

- [ ] 编译通过
- [ ] write/bash 工具触发时，ACP 客户端收到 `requestPermission` 事件
- [ ] 用户拒绝后工具中止执行
- [ ] 用户允许后工具正常执行
- [ ] `permission.cpp` 规则引擎中的 always_allow/always_deny 规则生效

---

## P0-3：修复 7 个失败测试

> **状态**：📋 待启动
> **依赖**：无
> **工时**：0.5 天
> **影响版本**：v2.x

### 问题描述

`system_prompt_test.cpp`（12 个断言）和 `prompt_builder_test.cpp`（2 个断言）失败。

**根因**：`system_prompt.cpp` 通过 `current_path() / "prompts"` 加载文件。测试运行工作目录为 `build/debug/tests/`，但 `copy_prompts` CMake target 将文件复制到 `build/debug/prompts/`（上级目录），导致路径不匹配，fallback 到内嵌字符串，缺少测试断言需要的 section。

### 修复方向

修改 `tests/CMakeLists.txt`，在 `copy_prompts` 自定义命令中同时将 prompts 复制到 `${CMAKE_BINARY_DIR}/tests/prompts/`：

```cmake
add_custom_command(
  TARGET copy_prompts POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E copy_directory
    ${CMAKE_SOURCE_DIR}/prompts
    ${CMAKE_BINARY_DIR}/prompts
  COMMAND ${CMAKE_COMMAND} -E copy_directory   # 新增
    ${CMAKE_SOURCE_DIR}/prompts                # 新增
    ${CMAKE_BINARY_DIR}/tests/prompts          # 新增
)
```

### 验收标准

- [ ] 编译通过（debug coverage build）
- [ ] 0 test failures（当前 7 failed）
- [ ] `system_prompt_test.cpp` 全部 12 个断言通过
- [ ] `prompt_builder_test.cpp` 全部 2 个断言通过

---

## P0-4：MCP 单元测试补全

> **状态**：📋 待启动
> **依赖**：无
> **工时**：2 天
> **影响版本**：v3.0

### 问题描述

`tests/unit/mcp/` 目录存在但 0 文件。计划 3.1 要求覆盖率 ≥ 80%，以下文件均无测试：

- `mcp.cpp`（MCP 核心）
- `client.cpp`（MCP 客户端）
- `stdio_transport.cpp`（stdio 传输层）
- `sse_transport.cpp`（SSE 传输层）
- `http_transport.cpp`（HTTP 传输层）

### 修复方向

新增以下测试文件：

| 测试文件                   | 覆盖目标                                                           |
| -------------------------- | ------------------------------------------------------------------ |
| `test_mcp_types.cpp`       | MCPStatus 枚举、MCPTool/Resource/Prompt 类型、工具名 sanitize 规则 |
| `test_mcp_manager.cpp`     | MCPManager 生命周期、MCPToolWrapper 命名规则验证                   |
| `test_stdio_transport.cpp` | stdio 读写协议帧、Content-Length framing                           |
| `test_mcp_client.cpp`      | 客户端请求/响应序列化、超时（30000ms 默认值）                      |

同步更新 `tests/CMakeLists.txt` 注册新测试目标。

### 验收标准

- [ ] 编译通过
- [ ] 所有新增测试通过
- [ ] MCP 模块整体行覆盖率 ≥ 80%

---

## P1-1：内置 Agent 实现

> **状态**：📋 待启动
> **依赖**：P0-1, P0-2, **P1-6**
> **工时**：3 天
> **影响版本**：v2.x + v3.0

### 问题描述

三个内置 Agent 均为占位，只返回格式化字符串：

```cpp
// build_agent.cpp / explore_agent.cpp / plan_agent.cpp
// Build/Explore/Plan agent is a placeholder for now
return ExecuteResult::ok(fmt::format("... processed..."));
```

`task_tool.cpp` line 167 直接调用 `agent->execute(exec_params)`，导致 `TaskTool` 派发子任务时获得的是占位结果，而非真实 LLM 输出。

### 修复方向

参考 OpenCode `agent.ts` 子 Agent 路由体系，每个 Agent 的 `execute()` 内部通过子 `SessionLoop` 接入真实 LLM：

**通用模式**（三个 Agent 共享）：

```cpp
ExecuteResult XxxAgent::execute(const ExecuteParams& params) {
    // 1. 从 ProviderManager 获取已注册的 Provider（P1-6 保证已初始化）
    auto& pm = provider::ProviderManager::instance();
    auto provider_opt = pm.get_default_provider();
    if (!provider_opt) {
        return ExecuteResult::error("No provider configured");
    }

    // 2. 创建子 SessionLoop（使用 session_id 对应的会话或新建）
    session::SessionLoop sub_loop(params.session_id);
    sub_loop.set_provider(provider_opt->get());
    sub_loop.set_model(/* agent 专属模型，或 provider 默认模型 */);
    sub_loop.set_agent(shared_from_this());  // 注入自身（携带 info_.prompt）

    // 3. 运行并收集结果
    auto result = sub_loop.run(params.prompt);
    if (result == session::LoopResult::Error) {
        return ExecuteResult::error("LLM execution failed");
    }
    auto last_text = sub_loop.messages().back().get_text();
    return ExecuteResult::ok(last_text);
}
```

**各 Agent 专属系统提示词**（设置在 `info_.prompt`）：

- **BuildAgent**：「你是代码构建专家。使用 bash/write_file 工具完成编译、测试、修复任务。」
- **ExploreAgent**：「你是代码探索专家（只读）。使用 read_file/glob/grep/list_dir 分析代码，不得写入任何文件。」
- **PlanAgent**：「你是任务规划专家。分析请求并输出详细的分步执行计划，不调用任何工具。」

**工具权限**（已在各 Agent 构造函数中定义，保持不变）：

- BuildAgent：允许所有工具（已有 Permission Ruleset）
- ExploreAgent：只允许 read_file/bash(ls/find/grep/git)，其余 Deny
- PlanAgent：无工具调用权限

### 验收标准

- [ ] 编译通过
- [ ] BuildAgent.execute() 返回真实 LLM 响应（含工具调用轨迹）
- [ ] ExploreAgent.execute() 返回实际代码分析结果（只读工具调用）
- [ ] PlanAgent.execute() 返回 LLM 生成的分步计划文本
- [ ] TaskTool 通过 Agent.execute() 获得真实子任务结果
- [ ] 各 Agent 有单元测试（Mock Provider）
- [ ] 端到端集成测试：TaskTool → Agent.execute() → 真实 LLM 响应（参见 P1-6）

---

---

## P1-6：Provider 初始化与端到端 LLM 验证

> **状态**：📋 待启动
> **依赖**：无（独立问题，P1-1 的前置）
> **工时**：1.5 天
> **影响版本**：v2.x + v3.0

### 问题描述

当前 `apps/turbot-cli/src/commands.cpp` 的 `run_session()` 从未为 `SessionLoop` 注入 Provider 和 Model，导致 `process_llm_response()` 的守卫逻辑直接返回 `LoopResult::Error`：

```cpp
// session_loop.cpp line 326-331
LoopResult SessionLoop::process_llm_response() {
    if (!provider_ || model_id_.empty()) {
        // 始终命中此分支 — 实际 LLM 调用从未发生
        return LoopResult::Error;
    }
    // ...
}

// commands.cpp run_session() — 缺失的注入代码
core::session::SessionLoop loop(session);
loop.set_agent(agent);
// ← 从未调用 loop.set_provider() / loop.set_model()
```

同时，`ProviderManager` 单例从未初始化，导致 Agent.execute() 内无法通过 `get_default_provider()` 获取 Provider 实例（P1-1 的依赖）。

`.turbot/turbot.json` 已包含完整的 bailian provider 配置（api_key + base_url + default_model），但从未被加载。

### 修复方向

**1. 在 `commands.cpp` 中加载配置并初始化 Provider**

```cpp
#include <turbot/core/provider/impl/bailian_provider.hpp>
#include <turbot/core/provider/provider_manager.hpp>
#include <nlohmann/json.hpp>
#include <fstream>

void init_provider_from_config(const std::string& config_path) {
    // 读取 .turbot/turbot.json
    std::ifstream f(config_path);
    if (!f.is_open()) return;
    auto cfg = nlohmann::json::parse(f);

    // 解析 providers 数组，找到 type == "bailian" 的条目
    for (const auto& p : cfg.value("providers", nlohmann::json::array())) {
        if (p.value("type", "") == "bailian") {
            provider::ProviderConfig config;
            config.api_key  = p.value("api_key", "");
            config.base_url = p.value("base_url", "");

            auto bailian = std::make_shared<provider::BailianProvider>(config);
            auto& pm = provider::ProviderManager::instance();
            pm.register_provider(bailian);
            pm.set_default_provider("bailian");
            break;
        }
    }
}
```

在 `run_session()` 和 `run_acp()` 开头调用 `init_provider_from_config(".turbot/turbot.json")`。

**2. 在 `run_session()` 中为 SessionLoop 注入 Provider 和 Model**

```cpp
core::session::SessionLoop loop(session);
loop.set_agent(agent);

// 注入 provider + model（从 ProviderManager 获取）
auto& pm = core::provider::ProviderManager::instance();
if (auto prov = pm.get_default_provider()) {
    loop.set_provider(prov->get());
    // model 从 turbot.json default_model 读取，或 hardcode 默认值
    loop.set_model("qwen3-coder-plus");
}
```

**3. 添加端到端集成测试**

在 `tests/integration/` 新增 `llm_provider_e2e_test.cpp`：

```cpp
// 测试场景 1：BailianProvider 直连验证
TEST_CASE("E2E-LLM-01: BailianProvider 直连调用", "[e2e][llm][bailian]") {
    // 跳过条件：环境变量 TURBOT_E2E 未设置时跳过（避免 CI 中产生费用）
    if (!std::getenv("TURBOT_E2E")) SKIP("Set TURBOT_E2E=1 to run LLM e2e tests");

    provider::ProviderConfig cfg;
    cfg.api_key  = std::getenv("BAILIAN_API_KEY") ?: "";
    cfg.base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1";

    provider::BailianProvider bailian(cfg);
    REQUIRE(bailian.is_ready());

    // 最简单的 chat 请求
    auto resp = bailian.chat(
        {provider::ChatMessage::user("Reply with exactly: HELLO")},
        "qwen3-coder-plus"
    );
    REQUIRE_FALSE(resp.is_error());
    REQUIRE_FALSE(resp.get_text().empty());
}

// 测试场景 2：SessionLoop + BailianProvider 完整链路
TEST_CASE("E2E-LLM-02: SessionLoop + BailianProvider 完整链路", "[e2e][llm][session-loop]") {
    if (!std::getenv("TURBOT_E2E")) SKIP("Set TURBOT_E2E=1 to run LLM e2e tests");

    // 初始化 provider
    provider::ProviderConfig cfg{ .api_key = std::getenv("BAILIAN_API_KEY") ?: "" };
    auto bailian = std::make_shared<provider::BailianProvider>(cfg);

    // 创建 SessionLoop 并注入
    session::SessionLoop loop("e2e-test-session");
    loop.set_provider(bailian.get());
    loop.set_model("qwen3-coder-plus");
    loop.set_agent(std::make_shared<agent::BuildAgent>());

    // 运行并验证
    auto result = loop.run("What is 1+1? Reply with the number only.");
    REQUIRE(result == session::LoopResult::Stop);
    REQUIRE_FALSE(loop.messages().empty());

    // 验证助手消息非空
    auto& msgs = loop.messages();
    auto it = std::find_if(msgs.rbegin(), msgs.rend(),
        [](const auto& m) { return m.role() == core::Role::Assistant; });
    REQUIRE(it != msgs.rend());
    REQUIRE_FALSE(it->get_text().empty());
}
```

测试通过 `TURBOT_E2E=1 BAILIAN_API_KEY=<key>` 环境变量控制，默认跳过，不影响 CI。

**4. 更新 `tests/CMakeLists.txt`**

将 `llm_provider_e2e_test.cpp` 加入 `turbot-e2e-tests` target 的 `E2E_TEST_SOURCES`。

### 验收标准

- [ ] 编译通过
- [ ] `run_session` 启动后 SessionLoop 可执行真实 LLM 调用（不再返回 config_error）
- [ ] `run_acp` 启动后 ACP 会话可接收真实 LLM 响应
- [ ] E2E-LLM-01：BailianProvider 直连调用返回非空响应
- [ ] E2E-LLM-02：SessionLoop 完整链路返回 LoopResult::Stop + 非空助手消息
- [ ] 默认（无 `TURBOT_E2E`）CI 跳过 E2E 测试，不产生网络请求

---

## P1-2：ACP cancel() 中断信号

> **状态**：📋 待启动
> **依赖**：P0-1
> **工时**：1 天
> **影响版本**：v3.0

### 问题描述

`libs/core/src/acp/agent.cpp` line 439-448，`cancel()` 方法为 no-op stub：

```cpp
void TurbotACPAgent::cancel(const CancelNotification& notif) {
    // No-op: cancel is handled implicitly via SessionLoop::stop()...
    // This stub satisfies the interface contract.
}
```

`SessionLoop` 无 stop handle 暴露给 `agent.cpp`，导致中断信号无法传递。

### 修复方向

1. 在 `SessionLoop` 中暴露 `stop()` 方法（或通过 `abort_flag_` 的公开接口）
2. `TurbotACPAgent` 持有对应 session 的 `SessionLoop` 弱引用或 stop callback
3. `cancel()` 内通过 session_id 查找对应 loop，调用 `stop()`

### 验收标准

- [ ] 编译通过
- [ ] 发送 cancel 通知后，运行中的 SessionLoop 在下一个工具调用边界停止
- [ ] cancel 后 ACP 侧收到正确的结束状态事件

---

## P1-3：set_on_tool_result diff 内容修复

> **状态**：📋 待启动
> **依赖**：无
> **工时**：1 天
> **影响版本**：v3.0

### 问题描述

`libs/core/src/acp/agent.cpp` line 384-390，edit 工具完成时 diff 块内容为空：

```cpp
// rawInput not available in result callback; emit empty-path diff
content.push_back({{"type", "diff"},
                   {"path", ""},
                   {"oldText", ""},
                   {"newText", ""}});
```

`load_session` 历史回放路径（line 609-615）已正确从 `args` 提取，弹流路径是主要问题。

### 修复方向

在工具结果回调中传入 `rawInput`（工具调用的原始参数）：

1. 扩展 `set_on_tool_result` 回调签名，携带 `tool_call_input`（含 `filePath`/`oldString`/`newString`）
2. 在 edit 工具完成时，从 `rawInput` 提取 `filePath`、`oldString`、`newString`
3. 生成真实 diff 块：`{"type": "diff", "path": filePath, "oldText": oldString, "newText": newString}`

### 验收标准

- [ ] 编译通过
- [ ] edit 工具执行后，ACP 客户端收到含真实路径和 diff 内容的事件
- [ ] IDE 侧可正确渲染文件变更 diff

---

## P1-4：generate_agent() LLM 接入

> **状态**：📋 待启动
> **依赖**：无
> **工时**：1.5 天
> **影响版本**：v2.x

### 问题描述

`agent_loader.cpp` line 265，`generate_agent()` 只做 slug 字符串处理，未接入 LLM：

```cpp
// This is a placeholder implementation
// In a full implementation, this would use LLM to generate agent configuration
```

### 修复方向

1. 接入 `LLM::complete()` 接口
2. 构建生成提示词：要求 LLM 根据用户描述输出 Agent 配置 JSON（name/description/system_prompt/tools 列表）
3. 解析 LLM 返回的 JSON，写入 Agent 配置文件
4. 注册到 `AgentRegistry`

### 验收标准

- [ ] 编译通过
- [ ] 传入自然语言描述后，返回合法的 Agent 配置 JSON
- [ ] 生成的 Agent 可被 `load_from_config` 正确加载
- [ ] Mock LLM 单元测试通过

---

## P1-5：v2.8 计划文档状态同步

> **状态**：📋 待启动
> **依赖**：无
> **工时**：0.5 天
> **影响版本**：文档

### 问题描述

`docs/plans/2.8/` 计划文档状态标记为「🚧 进行中」，但源码核查确认 P0/P1 差距已全部落地（RetryManager/EventBus/SessionRevert/prune/repairToolCall/Plugin/tool_choice/question/per-step token 均已完成）。

### 修复方向

1. 将 2.8 计划文档中 P0/P1 已落地条目标记为 ✅
2. 将整体文档状态从「进行中」更新为「✅ 已完成（P2/P3 不阻塞主流程）」
3. 同步 `docs/plans/README.md` 中 v2.8 的进度状态

### 验收标准

- [ ] 2.8 计划文档 P0/P1 条目全部标记 ✅
- [ ] README.md 导航中 v2.8 状态正确

---

## P2-1：高级工具实现

> **状态**：📋 待启动
> **依赖**：P0/P1 全部完成
> **工时**：3 天
> **影响版本**：v2.x

### 目标工具

| 工具         | 对齐 OpenCode 实现    | 描述                                       |
| ------------ | --------------------- | ------------------------------------------ |
| `multiedit`  | `tools/multiedit.ts`  | 批量多文件编辑，单次请求处理多个 edit 操作 |
| `webfetch`   | `tools/webfetch.ts`   | HTTP 请求抓取网页内容，返回 Markdown 格式  |
| `codesearch` | `tools/codesearch.ts` | 语义代码搜索（基于 AST 或向量索引）        |

### 验收标准

- [ ] 三个工具均在 `ToolRegistry` 中注册
- [ ] `multiedit` 可批量编辑多个文件
- [ ] `webfetch` 可抓取并返回 Markdown 内容
- [ ] `codesearch` 可按语义查询返回代码片段
- [ ] 各工具有单元测试

---

## P2-2：LSP 语言服务器扩展（3→8 种）

> **状态**：📋 待启动
> **依赖**：P0/P1 全部完成
> **工时**：2 天
> **影响版本**：v3.0

### 目标

当前支持：Clangd / Pyright / Gopls

参考 OpenCode `src/lsp/server.ts` 新增：

| 新增服务器         | 语言 / 工具                             |
| ------------------ | --------------------------------------- |
| Deno LSP           | TypeScript/JS（Deno 运行时）            |
| ts-language-server | TypeScript/JavaScript（Node 运行时）    |
| Vue LSP（volar）   | Vue.js                                  |
| ESLint LSP         | JavaScript/TypeScript lint              |
| Oxlint LSP         | JavaScript/TypeScript lint（Rust 实现） |
| Biome LSP          | JavaScript/TypeScript format + lint     |

实现位置：`libs/lsp/src/builtin_servers.cpp`，参照已有三种服务器的注册模式扩展。

### 验收标准

- [ ] 编译通过
- [ ] 8 种服务器配置均可被 `LSPManager` 正确加载
- [ ] 各服务器的启动命令/参数正确（参照 OpenCode server.ts 中的配置）
- [ ] 更新 `builtin_servers` 文档注释

---

## P2-3：SSE fallback 降级逻辑集成测试

> **状态**：📋 待启动
> **依赖**：P0/P1 全部完成
> **工时**：1 天
> **影响版本**：v3.0

### 问题描述

`HttpTransport` 实现了 Streamable HTTP → SSE fallback，但无 mock 服务器集成测试，降级条件（Content-Type 判断、超时逻辑）无法验证。

### 修复方向

1. 在 `tests/integration/mcp/` 中添加 mock HTTP 服务器（使用已有的 `http_client` 基础设施）
2. 模拟服务器返回非 SSE Content-Type，验证 `HttpTransport` 正确降级到 SSE 模式
3. 模拟超时场景，验证 fallback 超时处理逻辑

### 验收标准

- [ ] 编译通过
- [ ] fallback 路径测试通过（Content-Type 触发）
- [ ] 超时触发 fallback 测试通过

---

## 验收目标（SPEARM）

修复完成后预期评分提升：

| 版本 | 当前综合分         | 目标综合分  | 关键维度提升目标                                                  |
| ---- | ------------------ | ----------- | ----------------------------------------------------------------- |
| v2.x | 80/100             | **≥90/100** | S: B-→A，P: B+→A-，E: B+→A-，A: B+→A-，R: B-→B+，M: B→A-          |
| v3.0 | 70/100（上限锁定） | **≥90/100** | S: C+→A（权限闭环），R: C+→B+（Session+测试），E: B-→A-，解锁上限 |

### 达成 90 分所需的维度目标

**v2.x 计算路径**（公式：加权和 / 11 × 10）：

| 维度 | 当前等级 | 当前分值 | 目标等级 | 目标分值 | 提升条件                                            |
| ---- | -------- | -------- | -------- | -------- | --------------------------------------------------- |
| S    | B-       | 7.5      | **A**    | 9.5      | 权限注入闭环（P0-2）+ 权限规则覆盖测试              |
| P    | B+       | 8.5      | **A-**   | 9.0      | 性能测试补充（v2.9 覆盖率提升附带）                 |
| E    | B+       | 8.5      | **A-**   | 9.0      | Session 错误路径明确化（P0-1 完成后）               |
| A    | B+       | 8.5      | **A-**   | 9.0      | Session DB 接入完成，架构持久化层完整               |
| R    | B-       | 7.5      | **B+**   | 8.5      | 0 failed + 覆盖率 ≥85%（P0-3）+ Session DB          |
| M    | B        | 8.0      | **A-**   | 9.0      | Agent 占位消除（P1-1 + P1-6）+ 文档状态同步（P1-5） |

> 目标分：9.5×3 + 9.0×2 + 9.0×2 + 9.0×1.5 + 8.5×1.5 + 9.0×1 = 28.5+18+18+13.5+12.75+9 = **99.75 / 11 = 9.068 × 10 = 90.7 ≈ 91**

**v3.0 计算路径**：

| 维度 | 当前等级 | 当前分值 | 目标等级 | 目标分值 | 提升条件                                              |
| ---- | -------- | -------- | -------- | -------- | ----------------------------------------------------- |
| S    | C+       | 7.0      | **A**    | 9.5      | 权限注入完整闭环（P0-2），ACP→EventBus→工具守卫全通   |
| P    | B        | 8.0      | **B+**   | 8.5      | MCP 测试覆盖率达标后性能路径可验证                    |
| E    | B-       | 7.5      | **A-**   | 9.0      | Session get 错误路径明确（P0-1），ACP try-catch 完整  |
| A    | B+       | 8.5      | **A-**   | 9.0      | Session DB 接入，架构无占位破洞                       |
| R    | C+       | 7.0      | **B+**   | 8.5      | Session DB（P0-1）+ MCP 测试（P0-4）+ cancel（P1-2）  |
| M    | B        | 8.0      | **A-**   | 9.0      | 三个 Agent 占位消除（P1-1 + P1-6）+ diff 修复（P1-3） |

> 目标分：9.5×3 + 8.5×2 + 9.0×2 + 9.0×1.5 + 8.5×1.5 + 9.0×1 = 28.5+17+18+13.5+12.75+9 = **98.75 / 11 = 8.977 × 10 = 89.8 ≈ 90**
> S/R 均达 B+ 以上，上限解除，进入自由计算区间。

**解锁条件**：P0-1（Session DB）+ P0-2（权限注入）完成后，v3.0 S/R 维度均脱离 C+ 区间，综合分上限解除；P0-3（测试修复）+ P1-6（Provider 初始化）+ P1-1（Agent 实现）完成后，v2.x/v3.0 可进入 90 分区间。
