# Turbot-AI Provider系统实现计划

## 1. 概述

本计划是 Turbot-AI 项目的第五阶段实现，旨在建立Provider系统。Provider系统负责与AI提供商（如OpenAI、Anthropic等）进行交互，提供统一的接口来调用AI模型。

### 1.1 目标

- 实现Provider抽象接口
- 实现OpenAI提供商
- 实现模型管理
- 支持流式聊天
- 支持工具调用

### 1.2 依赖关系

- **前置依赖**: 1.1 - 基础架构, 1.3 - 网络层, 1.4 - 核心数据模型
- **后置依赖**: 会话系统、Agent系统

### 1.3 交付物

- Provider接口定义
- OpenAI提供商实现
- 模型管理器
- 流式聊天支持
- 单元测试

## 2. 架构设计

```
┌─────────────────────────────────────┐
│      Session/Agent层                 │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      ProviderManager                 │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      Provider接口                    │
└──────────────┬──────────────────────┘
               ↓
┌──────────────┬──────────────────────┐
│  OpenAI      │  Anthropic (未来)    │
│  Provider    │  Provider            │
└──────────────┴──────────────────────┘
```

## 3. 核心接口

### 3.1 Provider接口

```cpp
namespace turbot::core::provider {

struct ModelConfig {
    std::string model_id;
    std::string provider_id;
};

struct ModelCapabilities {
    bool temperature = false;
    bool reasoning = false;
    bool tool_call = false;
    bool streaming = false;
};

struct ModelInfo {
    std::string id;
    std::string provider_id;
    std::string name;
    ModelCapabilities capabilities;
    nlohmann::json pricing;
    nlohmann::json limits;
};

struct ChatMessage {
    enum class Role { System, User, Assistant, Tool };
    Role role;
    std::string content;
    std::optional<std::string> tool_call_id;
    std::optional<nlohmann::json> tool_calls;
};

struct ChatStreamEvent {
    enum class Type { TextDelta, ToolCall, Finish, Error };
    Type type;
    std::string content;
    nlohmann::json data;
};

class Provider {
public:
    virtual ~Provider() = default;

    virtual std::string name() const = 0;

    // 获取模型信息
    virtual Task<ModelInfo> get_model(const std::string& model_id) = 0;

    // 列出所有模型
    virtual Task<std::vector<ModelInfo>> list_models() = 0;

    // 聊天请求（流式）
    virtual AsyncGenerator<ChatStreamEvent> chat_stream(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options = {}
    ) = 0;

    // 聊天请求（非流式）
    virtual Task<ChatMessage> chat(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options = {}
    ) = 0;

    // 验证认证
    virtual Task<bool> authenticate() = 0;
};

} // namespace turbot::core::provider
```

### 3.2 ProviderManager

```cpp
class ProviderManager {
public:
    static ProviderManager& instance();

    void register_provider(std::shared_ptr<Provider> provider);

    std::shared_ptr<Provider> get_provider(const std::string& provider_id);

    std::vector<std::shared_ptr<Provider>> list_providers();

    Task<std::vector<ModelInfo>> list_models();

    Task<std::shared_ptr<Provider>> get_default_provider();

private:
    ProviderManager() = default;
    std::unordered_map<std::string, std::shared_ptr<Provider>> providers_;
};
```

## 4. OpenAI实现

```cpp
namespace turbot::core::provider::openai {

class OpenAIProvider : public Provider {
public:
    explicit OpenAIProvider(const std::string& api_key);

    std::string name() const override { return "openai"; }

    Task<ModelInfo> get_model(const std::string& model_id) override;

    Task<std::vector<ModelInfo>> list_models() override;

    AsyncGenerator<ChatStreamEvent> chat_stream(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options = {}
    ) override;

    Task<ChatMessage> chat(
        const std::vector<ChatMessage>& messages,
        const ModelConfig& config,
        const nlohmann::json& options = {}
    ) override;

    Task<bool> authenticate() override;

private:
    std::string api_key_;
    std::shared_ptr<network::http::HttpClient> http_client_;
};

} // namespace turbot::core::provider::openai
```

## 5. 使用示例

```cpp
// 创建OpenAI提供商
auto provider = std::make_shared<OpenAIProvider>("sk-xxx");

// 注册到管理器
ProviderManager::instance().register_provider(provider);

// 聊天
std::vector<ChatMessage> messages = {
    {ChatMessage::Role::User, "Hello, how are you?"}
};

ModelConfig config{"gpt-4", "openai"};

// 流式响应
for co_await (auto event : provider->chat_stream(messages, config)) {
    if (event.type == ChatStreamEvent::Type::TextDelta) {
        std::cout << event.content;
    }
}
```

## 6. 验收标准

- [ ] Provider接口定义完整
- [ ] OpenAI提供商正常工作
- [ ] 流式聊天正常
- [ ] 单元测试通过

## 7. 时间估算

| 任务 | 估算时间 |
|------|---------|
| Provider接口定义 | 1-2天 |
| OpenAI提供商实现 | 4-5天 |
| ProviderManager | 1-2天 |
| 单元测试 | 2-3天 |
| **总计** | **8-12天** |

## 8. 下一步

完成本计划后，进入**1.6 - 权限和工具系统实现计划**。

## 9. 参考文档

- OpenAI API文档: https://platform.openai.com/docs/api-reference
- OpenAI流式响应: https://platform.openai.com/docs/api-reference/chat/create