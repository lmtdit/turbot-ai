# 2.17 Agent 动态加载

> **状态**: ✅ 已完成
> **评级**: A
> **完成日期**: 2026-03-09
> **依赖**: 2.1 Agent 字段补充, 2.16 内置 Agent 完善
> **工时**: 1.5 天

## 完成总结

实现了 Agent 动态加载机制，包括初始化内置 Agents、从配置加载、重载等功能，并实现了 Agent 生成器。

### 实现文件

| 文件                                            | 说明                              |
| ----------------------------------------------- | --------------------------------- |
| `libs/core/include/turbot/core/agent/agent.hpp` | agent_loader/agent_generator 声明 |
| `libs/core/src/agent/agent_loader.cpp`          | Agent 加载器 + 生成器实现         |

### agent_loader 命名空间

```cpp
namespace agent_loader {

/// 初始化内置 agents（build, plan, explore）
size_t initialize_builtin_agents();

/// 从配置 JSON 加载 agents
size_t load_from_config(const nlohmann::json& config_json);

/// 重载所有 agents（清除后重新初始化）
size_t reload();

} // namespace agent_loader
```

### agent_generator 命名空间

```cpp
/// Agent 生成结果
struct AgentGenerateResult {
    std::string identifier;      // Agent 标识
    std::string when_to_use;     // 使用场景描述
    std::string system_prompt;   // 系统提示词

    AgentInfo to_agent_info() const;
    nlohmann::json to_json() const;
};

namespace agent_generator {

/// 生成新 agent 配置
AgentGenerateResult generate(const GenerateParams& params);

} // namespace agent_generator
```

### 实现功能对比

| 功能              | turbot-ai                                   | 说明              |
| ----------------- | ------------------------------------------- | ----------------- |
| Agent 初始化      | `agent_loader::initialize_builtin_agents()` | 初始化内置 agents |
| 获取指定 Agent    | `AgentRegistry::get(name)`                  | 获取指定 agent    |
| 列出所有 Agents   | `AgentRegistry::list()`                     | 列出所有 agents   |
| 获取默认 Agent    | `AgentRegistry::default_agent()`            | 获取默认 agent    |
| 生成新 Agent 配置 | `agent_generator::generate()`               | 生成新 agent 配置 |

### 配置加载格式

```json
{
  "build": {
    "disable": false,
    "model": { "model_id": "claude-3", "provider_id": "anthropic" },
    "temperature": 0.5,
    "steps": 50
  },
  "custom-agent": {
    "prompt": "You are a specialized agent...",
    "mode": "subagent"
  }
}
```

## 目标

实现 Agent 动态加载机制，支持从配置文件和代码动态创建 Agent。

## 输入

- Agent 基类
- AgentRegistry
- 配置系统

## 输出

- agent_loader 模块
- agent_generator 模块

## 测试覆盖

```cpp
// tests/unit/agent_test.cpp

TEST_CASE("agent_loader::initialize_builtin_agents") {
    AgentRegistry::instance().clear();
    size_t count = agent_loader::initialize_builtin_agents();
    REQUIRE(count >= 3);
    REQUIRE(AgentRegistry::instance().has("build"));
    REQUIRE(AgentRegistry::instance().has("plan"));
    REQUIRE(AgentRegistry::instance().has("explore"));
}

TEST_CASE("agent_loader::reload") {
    AgentRegistry::instance().clear();
    AgentRegistry::instance().register_agent(std::make_shared<TestAgent>("custom"));
    REQUIRE(AgentRegistry::instance().size() == 1);

    size_t count = agent_loader::reload();
    REQUIRE(count >= 3);
    REQUIRE_FALSE(AgentRegistry::instance().has("custom"));
    REQUIRE(AgentRegistry::instance().has("build"));
}

TEST_CASE("agent_generator::generate") {
    agent_generator::GenerateParams params;
    params.description = "Code review and optimization";

    auto result = agent_generator::generate(params);
    REQUIRE_FALSE(result.identifier.empty());
    REQUIRE_FALSE(result.when_to_use.empty());
    REQUIRE_FALSE(result.system_prompt.empty());
}
```

## 验收标准

- [x] 编译通过
- [x] 单元测试通过（170 断言，19 test cases）
- [x] 初始化内置 agents 正常
- [x] 从配置加载 agents 正常
- [x] 重载功能正常
- [x] Agent 生成器可用
- [x] 代码审查评级：A
